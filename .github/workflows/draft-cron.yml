name: Draft Cron

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes
  workflow_dispatch:  # Allow manual trigger

jobs:
  check-draft:
    runs-on: ubuntu-latest
    steps:
      - name: Check draft picks
        run: |
          curl -s -X GET "https://afl.gddevco.com/api/cron/draft" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            | cat
