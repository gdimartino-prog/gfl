This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
and refresh button
app/api/cuts/config/route.ts
app/api/cuts/route.ts
app/api/draft-picks/route.ts
app/api/draft-selection/route.ts
app/api/free-agents/route.ts
app/api/players/details/[id]/route.ts
app/api/players/route.ts
app/api/rosters/[team]/route.ts
app/api/rosters/[team]/routets.txt
app/api/teams/route.ts
app/api/test-players/route.ts
app/api/trades/route.ts
app/api/transactions/route.ts
app/content/constitution.md
app/cuts/page.tsx
app/draft/page.tsx
app/favicon.ico
app/globals.css
app/layout.tsx
app/page.tsx
app/rosters/page.tsx
app/rules/page.tsx
app/transactions/components/DropPlayer.tsx
app/transactions/components/FreeAgentPanel.tsx
app/transactions/components/IRPanel.tsx
app/transactions/components/TradePanel.tsx
app/transactions/page.tsx
components.json
components/Navbar.tsx
components/PlayerCard.tsx
components/SelectionModal.tsx
components/ui/button.tsx
components/ui/card.tsx
components/ui/checkbox.tsx
eslint.config.mjs
lib/config.ts
lib/draftPicks.ts
lib/freeAgency.ts
lib/googleSheets.ts
lib/playerLookup.ts
lib/players.ts
lib/transactions.ts
lib/transactionsts.txt
lib/utils.ts
next.config.ts
package.json
postcss.config.mjs
public/file.svg
public/globe.svg
public/next.svg
public/vercel.svg
public/window.svg
README.md
tailwind.config.js
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="and refresh button">
diff.astextplain.textconv=astextplain
filter.lfs.clean=git-lfs clean -- %f
filter.lfs.smudge=git-lfs smudge -- %f
filter.lfs.process=git-lfs filter-process
filter.lfs.required=true
http.sslbackend=schannel
core.autocrlf=true
core.fscache=true
core.symlinks=false
core.editor="C:\\Program Files\\Notepad++\\notepad++.exe" -multiInst -notabbar -nosession -noPlugin
pull.rebase=false
credential.helper=manager
credential.https://dev.azure.com.usehttppath=true
init.defaultbranch=master
user.net=George D
user.email=gdimartino@gmail.com
user.name=George Di Martino
core.repositoryformatversion=0
core.filemode=false
core.bare=false
core.logallrefupdates=true
core.symlinks=false
core.ignorecase=true
remote.origin.url=https://github.com/gdimartino-prog/gfl-fantasy.git
remote.origin.fetch=+refs/heads/*:refs/remotes/origin/*
branch.main.remote=origin
branch.main.merge=refs/heads/main
</file>

<file path="app/api/players/details/[id]/route.ts">
import { NextRequest, NextResponse } from 'next/server';
import { sheets, SHEET_ID } from '@/lib/googleSheets';

export async function GET(
  req: NextRequest, 
  { params }: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await params;
    const targetId = decodeURIComponent(id).toLowerCase().trim();

    const response = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Players', 
    });

    const rows: string[][] = response.data.values || [];
    const [headerRow, ...dataRows] = rows;

    // 1. Safe Header Mapping
    const headerMap = headerRow.reduce<Record<string, number>>((acc, col, idx) => {
      if (col) {
        // Convert to string first to prevent non-string header crashes
        acc[col.toString().toLowerCase().trim()] = idx;
      }
      return acc;
    }, {});

    // 2. Find the player
    const playerRow = dataRows.find((row) => {
      const first = (row[headerMap['first']] || '').trim();
      const last = (row[headerMap['last']] || '').trim();
      const age = (row[headerMap['age']] || '').trim();
      const offense = (row[headerMap['offense']] || '').trim();
      const defense = (row[headerMap['defense']] || '').trim();
      const special = (row[headerMap['special']] || '').trim();

      const identity = `${first}|${last}|${age}|${offense}|${defense}|${special}`.toLowerCase();
      return identity === targetId;
    });

    if (!playerRow) {
      console.log(`❌ Mismatch: Could not find ${targetId}`);
      return NextResponse.json({ error: 'Player Not Found' }, { status: 404 });
    }

    // 3. Safe Value Helper (Now handles strings only)
    const getVal = (colName: string, fallback: string = '0') => {
      const normalizedKey = colName.toString().toLowerCase().trim();
      const idx = headerMap[normalizedKey];
      
      if (idx !== undefined && playerRow[idx] !== undefined && playerRow[idx] !== '') {
        return playerRow[idx];
      }
      return fallback;
    };

    // 4. Build JSON using HEADER NAMES, not numbers
// 6. Build the JSON response with corrected Header Names
    return NextResponse.json({
      core: {
        team: getVal('team', 'FA'),
        first: getVal('first', 'Unknown'),
        last: getVal('last', 'Player'),
        age: getVal('age', '??'),
        pos: { 
          off: getVal('offense', ''), 
          def: getVal('defense', ''), 
          spec: getVal('special', '') 
        },
        uniform: getVal('uniform', '00'), // Or 'number'
      },
      ratings: {
        run_block: getVal('run block'), 
        pass_block: getVal('pass block'), 
        run_def: getVal('run defense'), 
        pass_def: getVal('pass defense'),
        pass_rush: getVal('pass rush'), 
        total_def: getVal('total defense'), // Matched your header!
        breakaway: getVal('breakaway'), 
        short_yard: getVal('short yardage'),
        audible: getVal('audible'), 
        pressure: getVal('pressure'),
        receiving: getVal('receiving'), 
        durability: getVal('durability')
      },
      contract: { 
        salary: getVal('salary', 'N/A'), 
        length: getVal('years', 'N/A') 
      },
      stats: {
        games: getVal('games'),
        rushing: { 
          att: getVal('rush attempts'), yds: getVal('rush yards'), 
          long: getVal('rush long'), td: getVal('rush TD')
        },
        passing: { 
          att: getVal('pass attempts'), comp: getVal('completions'), 
          yds: getVal('pass yards'), int: getVal('pass interceptions'), td: getVal('pass TD') 
        },
        defense: { 
          int: getVal('interceptions'), tackles: getVal('tackles'), 
          sacks: getVal('sacks'), stuffs: getVal('stuffs') 
        }
      }
    });

  } catch (error: any) {
    console.error("API ERROR:", error);
    return NextResponse.json({ 
      error: 'Server Error', 
      details: error.message 
    }, { status: 500 });
  }
}
</file>

<file path="app/api/rosters/[team]/routets.txt">
import { sheets, SHEET_ID } from '@/lib/googleSheets';

type RouteContext = {
  params: Promise<{ team: string }>;
};

export async function GET(req: Request, { params }: RouteContext) {
  try {
    const resolvedParams = await params;
    const teamShort = resolvedParams.team.toUpperCase();

    // Fetch data from both sheets
    const [playersRes, picksRes] = await Promise.all([
      sheets.spreadsheets.values.get({ 
        spreadsheetId: SHEET_ID, 
        range: 'Players!A:I' 
      }),
      sheets.spreadsheets.values.get({ 
        spreadsheetId: SHEET_ID, 
        range: 'DraftPicks!A:G' // Expanded range to A:G for the 7 columns
      })
    ]);

    const allPlayers = playersRes.data.values || [];
    const allPicks = picksRes.data.values || [];

    // 1. Process Players
    const roster = allPlayers
      .filter(row => row[0]?.toUpperCase() === teamShort)
      .map(row => ({
        // Using row[2] and row[3] for First Last name
        name: `${row[2]} ${row[3]}`,
        // Mapping positions from G, H, I columns
        pos: (row[6] || row[7] || row[8] || '??').toUpperCase(),
        group: row[6] ? 'OFF' : row[7] ? 'DEF' : 'SPEC'
      }));

    // 2. Process Draft Picks
    // Column Index Mapping: 0:Year, 1:Round, 3:Overall, 4:Original Team, 5:Current Owner
    const picks = allPicks
      .filter(row => row[4]?.toUpperCase() === teamShort) // Filter by CURRENT OWNER (Index 5)
      .map(row => ({
        year: row[0],
        round: row[1],
        overall: row[2],
        originalTeam: row[3],
        currentOwner: row[4]
      }))
      // Sort by Year first, then by Overall pick number
      .sort((a, b) => 
        Number(a.year) - Number(b.year) || 
        Number(a.overall) - Number(b.overall)
      );

    return Response.json({ roster, picks });
  } catch (error: any) {
    console.error("Roster API Error:", error);
    return Response.json({ error: error.message }, { status: 500 });
  }
}
</file>

<file path="app/api/test-players/route.ts">
import { NextResponse } from 'next/server';
import { sheets, SHEET_ID } from '../../../lib/googleSheets';

export async function GET() {
  try {
    const result = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Players',
    });

    return NextResponse.json({
      success: true,
      rows: result.data.values,
    });
  } catch (error: any) {
    return NextResponse.json(
      {
        success: false,
        error: error.message,
      },
      { status: 500 }
    );
  }
}
</file>

<file path="app/content/constitution.md">
**GFL Constitution**  
   
**Updated February 17, 2024**

**A.  Fees and League Management** 

A1.  The league fee is $250. 

* Each team must pay $250 by the start of the draft.   Any team which has an outstanding balance cannot participate in the draft.    

* GMs taking an **expansion** team play for free their first 2 seasons if they have **NO** GFL experience with Action  
* GMs taking an **existing** team play for free their first season if they have **NO** GFL experience with Action  
* GMs taking an **expansion** team play for free their first season if they have GFL experience with Action  
* GMs taking an **existing** team must pay if they have GFL experience with Action  
    
* A non-refundable $250 escrow fee will be paid by any new coach taking an expansion team or existing team.  This escrow fee will be applied to fees in future seasons 2nd or 3rd year depending on the points above. 

A2.  The SB winner will host the annual party and $600 is set aside for the party.  

A3.  Payments are generally made twice per season since many coaches exceed the $50 for free agent and injury pickup transactions.  Payments must be made within one week of the end of the GFL regular season.  

A4.  No league prize money can be paid until all league fees are paid. Coaches who do not pay the balance of their league fees are subject to league dismissal.   

A5.  The Commissioner is elected by the League Members and an election is held every 2 years before March 1st.   

A6.  The Commissioner will select a League Treasurer who will collect, hold, and issue payouts.  The Treasurer will keep track of league transactions along with the league statistician. 

A7.  The League Statistician is the keeper of the official league directory and provides updates to the directory including player transactions and game results.

A8.  The Commissioner will appoint an Assistant Commissioner who will rule on any matters where there is a conflict of interest with the Commissioner.  More than likely this would involve rulings that apply to the Commissioner’s team and the Commissioner’s actions as a Coach/GM.    

**B.  Draft & Roster Rules**  

B1. The GFL is a continuous ownership league that attempts to follow NFL rules where possible.    

Rosters.  During the GFL season, 46 players can be active for a game with a maximum roster size of 53 not including injured or Injured Reserve (IR) players.  This is subject to change based on NFL rules.  

Rosters may exceed the limit due to injured players who are Inactive and due to players (un-carded or injured during a GFL season) who are placed on IR.  All other players beyond the aforementioned must be inactive for a game.  A player's status set at the beginning of the game cannot change.  He cannot be switched from Active to Inactive or vice versa once a game begins. 

B2. Only players listed in the "Action PC Football" game for the season being utilized may be drafted**.**  

B3. At the end of the GFL season, each GM will submit a 30-man protected roster, 8 other pullback players who will be kept if there is no expansion, and will specifically list out all other players who are cuts.  The cut list is required to minimize the chance of any confusion.  This is due on March 1 at 12:00 AM.  The 30 men protected roster and 8 other players will be retained if there is no expansion.  If there are 2 expansion teams, then 26 players will be protected and 12 other players identified as pullbacks 

B4.  Any GM who misses the deadline for submitting his protected list and pullback list is subject to losing draft picks in the upcoming draft. If a team goes over the cuts deadline period which will be midnight March 1st  same date as expansion team deadline, they lose their 5th round draft pick (they are skipped ). If the team continues to not set keepers after 1 week, instead of the 5th they lose their 4th. And it will continue on like this:  
2 weeks \- third round instead of 5th or 4th.   
3 weeks \- 2nd round  
4 weeks \- 1st round. 

B5. GMs can protect a max of 2 QBs on their 38-man roster.  All other positions have no max on the 30 man list.   

B6. Any player may be retained on a roster from one year to the next provided he is included on the 30-man roster submitted at the end of the season. This includes players that are injured in the NFL season, which may result in that player not being carded in the upcoming GFL season.   

B7. There can be a maximum of 5 players on IR per team. Once a player is placed on IR he is out for the GFL season and cannot be used again under any circumstance.  To be placed on IR, the player must either not be carded or must sustain an injury in the current GFL season.  Note that this rule is not in line with the NFL rule which was changed in 2012 and then again in 2018\.  There is no applicability in the GFL for making this change.    

*\[The NFL and the NFL Players Association agreed to a short-term IR rule change which took effect in 2012\. The change allowed one player (per team) to be activated from the IR list, and was changed again in 2018 so each team can return up to two players during the season.\]* 

B8.  Players on the IR list that were un-carded and players placed on IR that were injured during the GFL season (players drafted or acquired via free-agent pickup or trade) can be protected on post-season 30 and 38 man rosters.  Injury pickups that suffer a GFL injury cannot be placed on IR and cannot be protected on post-season 30 and 38 man rosters.     

B9.  After the PC Action Football ratings are produced and after any expansion draft that may occur, there will be a Free Agent Draft which begins the official release date of the Action game that will consist of an unlimited number of rounds.  

B10.  Starting with the 1st round there is a time limit of 24 hours to make a pick. The 24-hour clock starts when the pick is posted by the preceding coach.  Starting with the 3rd round there is a time limit of 12 hours to make a pick. The 12-hour clock starts when the pick is posted by the preceding coach.  If a GM time expires, they will automatically miss future picks unless they have filled the missing pick.  If a GM time exprires 3 or more rounds in the draft, they will be automatically be skipped in subsequent rounds. They can still draft players, but they will have 0 time to make picks. 

B11. After the Free Agent Draft has concluded, GMs may sign players off of the free agent list.  The roster limit must be maintained at all times so a roster cut may be necessary. Released players are put in the free-agent pool.  Injured players (in the current GFL season) and players on IR do not count towards the roster limit total.  All monies generated post-draft will be added to the post-season payout figures.  

B12. Draft Order is determined by NFL Rule. 

* If there is an expansion team, that team will pick first, and if there are two expansion teams, they will flip a coin with one team getting the first pick in the draft and the other in the expansion draft of current players.  

B13.  The draft can start anytime after the finalized game is released and new season divisions have been set..  The draft clock starts 1 week after the final game is released.  This allows 2nd and subsequent pick to have no time limit until 1 week.

B14. The draft is over once every team has not made a pick 48 hours after the last pick.  (Note that the draft usually lasts 20 rounds).

B15.  Divisions will be set prior to the current season draft.  The number of divisions will be as follows:  

* 10-12 teams 	\- 2 divisions  
* 13 		\- 3 divisions  
* 14 	 	\- 2 divisions  
* 15		\- 3 divisions  
* 16 		\- 4 divisions  
* 17		\- 1 division  
* 18		\- 3 divisions  
* 19-20		\- 4 divisions


**C. Expansion.** 

C1. There can be a maximum of two expansion teams per year. Expansion teams must be in deadplace by March 1.  The expansion coach must pick his team, and draft his own players.  This means each existing team may lose 6 players (3 per expansion team) from their cut-list (players 31-53 or current NFL roster limit) except for an expansion team(s) entering their second year.   The cut-list from the expansion teams will include players 39-53 or the current NFL roster limit (5x).

C2. The expansion team needs to have a minimum of 38 players after the expansion draft and can have a max of the NFL roster limit.     

C3. The expansion team will be given a one-year grace period on losing players to expansion if there is expansion the following season. They will not have to cut their roster to 30 at year-end but instead will reduce their roster to 38 which will be their rosters entering the GFL regular draft.  They can lose players (39-5x) to the new expansion team(s).  An Expansion team will not be allowed to protect more than 4 QBs on their 38 man protected lists.  All other positions have no max.  

C4. In the event an existing coach drops out of the league, the new coach will have the option of taking over any franchise that no longer has a coach or drafting a team via the expansion rules. If there is more than one existing franchise available the expansion coach(es) can either:

* draw to see who gets the first choice of what team;

* combine the available teams and conduct a draft

**D. Trades**

D1. Trading Deadline: Trading for the current GFL season ends at same time as NFL trading dedline. 

D2.  Rules Governing Trades:** **

1. A trade is considered official when both coaches confirm the trade on the GFL message board or when the trade is made and jointly announced at a league function such as the Super Bowl party.  
     
2. All conditions of the trade must be specified at the time of the trade.  If conditions are not specified, then it is assumed that none exist.   
     
3. Under extraordinary circumstances where a trade is deemed completely one-sided and thus not made for competitive reasons, a league review of the trade can be initiated by the Commissioner.

D3. A trade effective for the following season may be made anytime with the following provisions:

1. The players involved in the trade must be accounted for on the 30 man rosters that are submitted at the end of the season.  Example: Green Bay makes a trade effective next season with Detroit and agrees to trade an OLB to Detroit for Detroit's first-round pick in next year's draft.  The OLB must be accounted for on Green Bay's 30 man roster submitted at year-end.  	

2. If a team quits the league in that upcoming year, the remaining team will not lose the pick.  The remaining team will select where the quitting team would have picked.  *So if I trade a LB for a number one but that team quits the league, then I get the pick that WOULD HAVE been in place had that team not quit.  The pick would be somewhere between 2 teams that remain in the league.*  

D4. $10.00 FREE AGENT PICKUPS: A free-agent at any position may be signed up until the NFL trading deadline for $10.  If the free-agent signing takes the roster above the NFL roster limit, (not including injured players or players on IR) then a cut MUST BE submitted with the signing.  If there is no cut specified then the signing cannot take place and will not be recognized.  All FA signings and cuts must be made on the GFL email group.  

D5. INJURY PLAYER PICKUPS: 

1. A $0.00 Interim Injury pick-up can be made with the stipulation that the player signed must play the same position as the injured player and be returned to the Free Agent pool when the Injured Player is healed.	 A team's roster may exceed the 53 player limit when there is an injury pick-up until the injured player heals.    
     
2. ~~A $0.00 Interim Injury pick-up can be made with the stipulation that the player signed does not have to play the same position as the injured player and be returned to the Free Agent pool when the Injured Player is healed.	 A team's roster may exceed the 53 player limit when there is an injury pick up until the injured player heals.~~    ***This rule was changed by vote.  Injury pickup must be at same position.  4/14/2024***  
     
3. INJURY PLAYER PICKUP RETAINED: A player picked-up due to an injury may be retained at no additional cost for more than one injury when another player on that team is hurt in a subsequent game.   When the player that was injured in the subsequent game is healed, you must then cut the player picked-up.  
     
4. No injury pickups can be included on post season 30 or 38 man rosters.  

   

D6.  Injury rule pertaining to games unavailable for the injured player.  
	

1. An injured player will miss the subsequent weeks in the schedule from the week in the schedule from which his injury occurred. e.g. Player X gets injured for 4 games playing for Team A in Team A's Week 3 game.  Player X will be injured for GFL Schedule weeks 4 thru 7 and is eligible to play in Week 8\.  This is irrespective of whether Player X is a free agent or signed by another team after the injury occurred who might be playing in another scheduled week.

2. A player can never play in a scheduled week prior to or equal to one which he has already played.  e.g. Player X's last game played was for Team A in a Week 4 game.  Player X cannot play a game for another team in any week prior to Week 5\.


**E. Protocol** 

E1. League business will be recorded on the GFL message board.  

E2. Coaches must play their games within the week they are scheduled or their computer coach (which they can program) will play the game. Of course exceptions can be made when there is a good reason and the commissioner is notified in advance. The delay cannot go beyond the 2nd week from the scheduled game. Example: Game scheduled for Monday Sept. 1\. You have until Monday Sept. 8 to play the game or the Computer Coach will play.  Coaches of existing GFL teams are prohibited to coach other GFL teams.  

E3. An unexcused failure to play the scheduled game could result in the Commissioner determining that the computer coach should play the game for the absent Coach depending on the situation.  

E4. A Coach who quits playing during a game without good reason will have the computer coach finish the game for him.  If the quitting team is the host then he must forward the game file to his opponent immediately. The Commissioner will rule on whether the offending Coach should have his hosting privileges suspended.  

E5. A Coach who explicitly or obviously quits the league will have his remaining games played by the computer coach or by a potential new Coach.  As per Rule E2 the games cannot be played by an existing Coach.  

E6. The treasurer will collect all money for league fees, including league entry and player transaction fees that must be paid.  

E7.  If there is a dispute during a game both coaches must agree before a play can be done over.  Common sense and fairness should be the rule.  Examples: If a non-kicker attempts and misses a FG or Extra Point then it is obvious that play should be re-done with the Kicker making the attempt.  If an Option Pass is attempted and the passer turns out to be a player not chosen and he throws an interception, then it should be assumed that the game is working properly and the text is incorrect.  That play should stand.  

E8.  It is the responsibility of each Coach to see that his lineups are correct at all times.  A coach cannot assume that he has the right to re-do a play because he notices after the fact that his lineups are incorrect.  

E9. The Commissioner is the sole person that can post league polls.  All league members should request that the Commissioner post a league poll.  

E10.  The results of a vote are effective for at least one GFL season.  No other vote on the same issue can be taken until one season is completed where the results of said vote is enforced.  

E11.  For all GFL polls where there are more than two (2) choices, there must be a plurality of 50% & 1 vote in order for a choice to win. In the event no one choice achieves the plurality, the top two choices in accumulating votes would then have a runoff vote.  

E12.  All on-line polls having to do with league rule change proposals must have a two week time limit as to when the poll closes.  The Commissioner will follow up with Coaches that have not yet cast a vote.  

E13.  In order for a poll result to be official at least half the members of the league must vote. If there is a poll in which less than half the members have voted at the time the Poll is closed, then the result will be considered null and the status quo will remain.  

E14.   Press Box Narrator must be turned off when playing online opponent.  This can only be turned on if both coaches agree. (can be turned off on Setup Screen, Sound tab)  

E15.  Teams must deploy a 46 man active roster that is healthy. Teams that do not maintain a 46 man active healthy roster will have a player chosen to their team from free agency to make their roster legal. The player chosen will be the highest rated free agent player at that position. The action taken to make a roster legal ( 46 man healthy roster ) if a team fails to do it will be done by league commissioner or assistant commissioner.

**F. Post Season**. 

F1. The number of teams that qualify for the playoffs is based on the number of teams in the league and the format of the divisions. This is announced prior to the start of the draft and is based on NFL Rules.  For 2024 season which has 18 teams, there will be 8 playoff teams. Use past season if not decided prior to season starting. 

F2. GFL regular season stats and injuries do carry over to the playoffs as per the operation of the Action Football game and the Playoff Module which is used.  Coaches are encouraged to monitor player usage as it compares to their actual NFL stats 

F3. The Action Playoff Mode will be used for post season play unless circumstances such as an inability to create the seedlings properly or game malfunction dictate otherwise. 

F4. Playoff seeding is the same as the NFL.  Division winner gets a home field game. In some cases depending on division format, a Wild Card with the best record amongst wild card teams may secure a home field game.  In some seasons, BYES are in place.  ***We follow NFL Rules.***  

F5. NFL tie-breaker rules are in effect to determine playoff seeding.  
NFL Tie-breaker rules (always confirm that the latest NFL rules are in effect***):***  

F6. The Super Bowl is played at the stadium where the current NFL Super Bowl is being played and the Home Field Advantage flag will be set to OFF.  There will be a one week rest period prior to the Super Bowl, if the NFL had an off week the current year.  

F7. Payouts are determined at the beginning of the season and the League Treasurer notifies the league of the details. Since player transaction money is accumulated throughout the season, exact figures will be determined at the end of the GFL regular season.  

**G. GAME RULES AND SETTINGS** 

Follows league precedent.  Any changes must be voted on prior to a new season’s draft.
</file>

<file path="app/globals.css">
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
  --color-sidebar-ring: var(--sidebar-ring);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar: var(--sidebar);
  --color-chart-5: var(--chart-5);
  --color-chart-4: var(--chart-4);
  --color-chart-3: var(--chart-3);
  --color-chart-2: var(--chart-2);
  --color-chart-1: var(--chart-1);
  --color-ring: var(--ring);
  --color-input: var(--input);
  --color-border: var(--border);
  --color-destructive: var(--destructive);
  --color-accent-foreground: var(--accent-foreground);
  --color-accent: var(--accent);
  --color-muted-foreground: var(--muted-foreground);
  --color-muted: var(--muted);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-secondary: var(--secondary);
  --color-primary-foreground: var(--primary-foreground);
  --color-primary: var(--primary);
  --color-popover-foreground: var(--popover-foreground);
  --color-popover: var(--popover);
  --color-card-foreground: var(--card-foreground);
  --color-card: var(--card);
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: calc(var(--radius) - 2px);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) + 4px);
  --radius-2xl: calc(var(--radius) + 8px);
  --radius-3xl: calc(var(--radius) + 12px);
  --radius-4xl: calc(var(--radius) + 16px);
}

:root {
  --radius: 0.625rem;
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.145 0 0);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.145 0 0);
  --primary: oklch(0.205 0 0);
  --primary-foreground: oklch(0.985 0 0);
  --secondary: oklch(0.97 0 0);
  --secondary-foreground: oklch(0.205 0 0);
  --muted: oklch(0.97 0 0);
  --muted-foreground: oklch(0.556 0 0);
  --accent: oklch(0.97 0 0);
  --accent-foreground: oklch(0.205 0 0);
  --destructive: oklch(0.577 0.245 27.325);
  --border: oklch(0.922 0 0);
  --input: oklch(0.922 0 0);
  --ring: oklch(0.708 0 0);
  --chart-1: oklch(0.646 0.222 41.116);
  --chart-2: oklch(0.6 0.118 184.704);
  --chart-3: oklch(0.398 0.07 227.392);
  --chart-4: oklch(0.828 0.189 84.429);
  --chart-5: oklch(0.769 0.188 70.08);
  --sidebar: oklch(0.985 0 0);
  --sidebar-foreground: oklch(0.145 0 0);
  --sidebar-primary: oklch(0.205 0 0);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.97 0 0);
  --sidebar-accent-foreground: oklch(0.205 0 0);
  --sidebar-border: oklch(0.922 0 0);
  --sidebar-ring: oklch(0.708 0 0);
}

.dark {
  --background: oklch(0.145 0 0);
  --foreground: oklch(0.985 0 0);
  --card: oklch(0.205 0 0);
  --card-foreground: oklch(0.985 0 0);
  --popover: oklch(0.205 0 0);
  --popover-foreground: oklch(0.985 0 0);
  --primary: oklch(0.922 0 0);
  --primary-foreground: oklch(0.205 0 0);
  --secondary: oklch(0.269 0 0);
  --secondary-foreground: oklch(0.985 0 0);
  --muted: oklch(0.269 0 0);
  --muted-foreground: oklch(0.708 0 0);
  --accent: oklch(0.269 0 0);
  --accent-foreground: oklch(0.985 0 0);
  --destructive: oklch(0.704 0.191 22.216);
  --border: oklch(1 0 0 / 10%);
  --input: oklch(1 0 0 / 15%);
  --ring: oklch(0.556 0 0);
  --chart-1: oklch(0.488 0.243 264.376);
  --chart-2: oklch(0.696 0.17 162.48);
  --chart-3: oklch(0.769 0.188 70.08);
  --chart-4: oklch(0.627 0.265 303.9);
  --chart-5: oklch(0.645 0.246 16.439);
  --sidebar: oklch(0.205 0 0);
  --sidebar-foreground: oklch(0.985 0 0);
  --sidebar-primary: oklch(0.488 0.243 264.376);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.269 0 0);
  --sidebar-accent-foreground: oklch(0.985 0 0);
  --sidebar-border: oklch(1 0 0 / 10%);
  --sidebar-ring: oklch(0.556 0 0);
}

@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground;
  }
}
</file>

<file path="components.json">
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "iconLibrary": "lucide",
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "registries": {}
}
</file>

<file path="components/PlayerCard.tsx">
import React from 'react';

interface PlayerCardProps {
  data: any;
  onClose: () => void;
}

const PlayerCard: React.FC<PlayerCardProps> = ({ data, onClose }) => {
  if (!data) return null;

  const core = data.core || data;
  const position = core.pos?.off || core.pos?.def || core.pos?.special || data.offense || 'ATH';
  
  // Metadata to ignore
  const excluded = ['id', 'identity', 'image', 'core', 'ratings', 'contract', 'uniform', 'team', 'offense', 'defense', 'special'];

  return (
    <div className="fixed inset-0 z-[100] flex items-center justify-center p-4 bg-slate-950/95 backdrop-blur-md animate-in fade-in duration-200">
      <div className="bg-[#020617] w-full max-w-6xl rounded-[2.5rem] border-2 border-slate-700 shadow-2xl overflow-hidden max-h-[90vh] flex flex-col">
        
        {/* HEADER */}
        <div className="p-6 bg-slate-900 border-b border-slate-700 flex justify-between items-center">
          <div className="flex flex-col">
            <div className="flex items-center gap-2 mb-1">
              <span className="bg-blue-600 text-white text-[10px] font-black px-2 py-0.5 rounded uppercase shadow-md">
                {position}
              </span>
              <span className="text-white font-black text-[10px] uppercase tracking-widest">
                AGE {core.age || '??'} // {core.college || 'Ratings & Stats'}
              </span>
            </div>
            <h2 className="text-4xl font-black italic uppercase text-white tracking-tighter leading-none">
              {core.first} <span className="text-blue-400">{core.last}</span>
            </h2>
          </div>
          <button onClick={onClose} className="p-3 bg-slate-800 hover:bg-red-500 rounded-full text-white transition-all border border-slate-600">
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="3" d="M6 18L18 6M6 6l12 12"/>
            </svg>
          </button>
        </div>

        {/* MAIN CONTENT AREA */}
        <div className="flex-1 overflow-y-auto p-8 grid grid-cols-1 lg:grid-cols-12 gap-8 custom-scrollbar">
          
          {/* LEFT COLUMN: Physical Ratings */}
          <div className="lg:col-span-4 space-y-8">
            <section>
              <h4 className="text-blue-400 font-black text-[12px] uppercase tracking-[0.2em] mb-4 italic">Physical Ratings</h4>
              <div className="grid grid-cols-1 gap-2">
                {data.ratings && Object.entries(data.ratings).map(([key, val]: any) => {
                  if (Number(val) === 0) return null;
                  return <RatingRow key={key} label={key} value={val} />;
                })}
              </div>
            </section>
          </div>

          {/* RIGHT COLUMN: Performance Data (Deep Search for Offense/Defense) */}
          <div className="lg:col-span-8 space-y-8 lg:border-l lg:border-slate-700 lg:pl-8">
            <h4 className="text-blue-400 font-black text-[12px] uppercase tracking-[0.2em] italic">Field Performance Breakdown</h4>
            
            <div className="space-y-10">
              {Object.entries(data)
                .filter(([key]) => !excluded.includes(key) && typeof data[key] === 'object')
                .map(([category, stats]: [string, any]) => {
                  
                  // Collect all non-zero stats recursively
                  const activeStats: {label: string, value: any}[] = [];
                  
                  const extractStats = (obj: any, prefix = '') => {
                    Object.entries(obj).forEach(([k, v]) => {
                      if (v && typeof v === 'object' && !Array.isArray(v)) {
                        extractStats(v, `${prefix}${k} `);
                      } else if (Number(v) > 0) {
                        activeStats.push({ label: `${prefix}${k}`, value: v });
                      }
                    });
                  };

                  extractStats(stats);

                  if (activeStats.length === 0) return null;

                  return (
                    <div key={category} className="space-y-4">
                      <p className="text-white font-black italic text-md uppercase border-l-4 border-emerald-500 pl-3">
                          <span className="tracking-widest">{category}</span>
                      </p>
                      <div className="flex flex-wrap gap-3">
                        {activeStats.map((stat, idx) => (
                          <DataChip key={idx} label={stat.label} value={stat.value} />
                        ))}
                      </div>
                    </div>
                  );
                })}
            </div>
          </div>
        </div>

        {/* FOOTER */}
        <div className="p-8 bg-slate-900 border-t border-slate-700">
           <button onClick={onClose} className="w-full py-5 bg-blue-600 hover:bg-blue-500 text-white font-black uppercase text-xs tracking-[0.4em] rounded-2xl transition-all shadow-xl active:scale-95">
             Close Profile
           </button>
        </div>
      </div>
    </div>
  );
};

function DataChip({ label, value }: { label: string, value: any }) {
    return (
        <div className="bg-slate-800 border border-slate-600 px-5 py-4 rounded-2xl flex flex-col min-w-[120px] shadow-lg border-b-4 border-b-emerald-500/20 hover:border-emerald-500 transition-colors">
            <span className="text-blue-200 text-[10px] font-black uppercase leading-none mb-2 tracking-wide">
                {label.replace(/_/g, ' ')}
            </span>
            <span className="text-emerald-400 text-3xl font-black tabular-nums italic leading-none">
                {value?.toString() || '0'}
            </span>
        </div>
    );
}

function RatingRow({ label, value }: { label: string, value: any }) {
  return (
    <div className="bg-slate-800/80 border border-slate-600 p-3 rounded-xl flex justify-between items-center px-5 group hover:border-emerald-500/50 transition-colors">
      <span className="text-blue-100 text-[11px] font-black uppercase tracking-tight">
        {label.replace(/_/g, ' ')}
      </span>
      <span className="text-emerald-400 text-xl font-black italic tabular-nums">{value ?? '0'}</span>
    </div>
  );
}

export default PlayerCard;
</file>

<file path="components/ui/button.tsx">
import * as React from "react"
import { Slot } from "@radix-ui/react-slot"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

const buttonVariants = cva(
  "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium transition-all disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 shrink-0 [&_svg]:shrink-0 outline-none focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px] aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground hover:bg-primary/90",
        destructive:
          "bg-destructive text-white hover:bg-destructive/90 focus-visible:ring-destructive/20 dark:focus-visible:ring-destructive/40 dark:bg-destructive/60",
        outline:
          "border bg-background shadow-xs hover:bg-accent hover:text-accent-foreground dark:bg-input/30 dark:border-input dark:hover:bg-input/50",
        secondary:
          "bg-secondary text-secondary-foreground hover:bg-secondary/80",
        ghost:
          "hover:bg-accent hover:text-accent-foreground dark:hover:bg-accent/50",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        default: "h-9 px-4 py-2 has-[>svg]:px-3",
        sm: "h-8 rounded-md gap-1.5 px-3 has-[>svg]:px-2.5",
        lg: "h-10 rounded-md px-6 has-[>svg]:px-4",
        icon: "size-9",
        "icon-sm": "size-8",
        "icon-lg": "size-10",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
)

function Button({
  className,
  variant = "default",
  size = "default",
  asChild = false,
  ...props
}: React.ComponentProps<"button"> &
  VariantProps<typeof buttonVariants> & {
    asChild?: boolean
  }) {
  const Comp = asChild ? Slot : "button"

  return (
    <Comp
      data-slot="button"
      data-variant={variant}
      data-size={size}
      className={cn(buttonVariants({ variant, size, className }))}
      {...props}
    />
  )
}

export { Button, buttonVariants }
</file>

<file path="components/ui/card.tsx">
import * as React from "react"

import { cn } from "@/lib/utils"

function Card({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card"
      className={cn(
        "bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm",
        className
      )}
      {...props}
    />
  )
}

function CardHeader({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-header"
      className={cn(
        "@container/card-header grid auto-rows-min grid-rows-[auto_auto] items-start gap-2 px-6 has-data-[slot=card-action]:grid-cols-[1fr_auto] [.border-b]:pb-6",
        className
      )}
      {...props}
    />
  )
}

function CardTitle({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-title"
      className={cn("leading-none font-semibold", className)}
      {...props}
    />
  )
}

function CardDescription({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-description"
      className={cn("text-muted-foreground text-sm", className)}
      {...props}
    />
  )
}

function CardAction({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-action"
      className={cn(
        "col-start-2 row-span-2 row-start-1 self-start justify-self-end",
        className
      )}
      {...props}
    />
  )
}

function CardContent({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-content"
      className={cn("px-6", className)}
      {...props}
    />
  )
}

function CardFooter({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="card-footer"
      className={cn("flex items-center px-6 [.border-t]:pt-6", className)}
      {...props}
    />
  )
}

export {
  Card,
  CardHeader,
  CardFooter,
  CardTitle,
  CardAction,
  CardDescription,
  CardContent,
}
</file>

<file path="components/ui/checkbox.tsx">
"use client"

import * as React from "react"
import * as CheckboxPrimitive from "@radix-ui/react-checkbox"
import { CheckIcon } from "lucide-react"

import { cn } from "@/lib/utils"

function Checkbox({
  className,
  ...props
}: React.ComponentProps<typeof CheckboxPrimitive.Root>) {
  return (
    <CheckboxPrimitive.Root
      data-slot="checkbox"
      className={cn(
        "peer border-input dark:bg-input/30 data-[state=checked]:bg-primary data-[state=checked]:text-primary-foreground dark:data-[state=checked]:bg-primary data-[state=checked]:border-primary focus-visible:border-ring focus-visible:ring-ring/50 aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive size-4 shrink-0 rounded-[4px] border shadow-xs transition-shadow outline-none focus-visible:ring-[3px] disabled:cursor-not-allowed disabled:opacity-50",
        className
      )}
      {...props}
    >
      <CheckboxPrimitive.Indicator
        data-slot="checkbox-indicator"
        className="grid place-content-center text-current transition-none"
      >
        <CheckIcon className="size-3.5" />
      </CheckboxPrimitive.Indicator>
    </CheckboxPrimitive.Root>
  )
}

export { Checkbox }
</file>

<file path="eslint.config.mjs">
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
</file>

<file path="lib/config.ts">
import { sheets, SHEET_ID } from './googleSheets';

export type Coach = {
  coach: string;
  team: string;
  teamshort: string;
  isCommissioner: boolean;
};

// Reads config tab and returns all coaches
export async function getCoaches(): Promise<Coach[]> {
  const res = await sheets.spreadsheets.values.get({
    spreadsheetId: SHEET_ID,
    range: 'Config',
  });

  const rows = res.data.values || [];

  // Assuming columns: team | teamshort | coach | isCommissioner
  const coaches: Coach[] = rows.slice(1).map(r => ({
    team: r[0] || '',
    teamshort: r[1] || '',
    coach: r[2] || '',
    isCommissioner: r[3] === 'TRUE' || r[3] === 'true',
  }));

  return coaches;
}
</file>

<file path="lib/freeAgency.ts">
import { sheets, SHEET_ID } from './googleSheets';
import { parsePlayers, Player } from './players';

/**
 * Execute a free agent pickup + waive transaction
 */
export async function executeFreeAgentMove(
  team: string,
  addIdentity: string,
  dropIdentity: string
) {
  // 1. Load players
  const res = await sheets.spreadsheets.values.get({
    spreadsheetId: SHEET_ID,
    range: 'Players',
  });

  const rows = res.data.values || [];
  const players = parsePlayers(rows);

  const addPlayer = players.find(p => p.identity === addIdentity);
  const dropPlayer = players.find(p => p.identity === dropIdentity);

  if (!addPlayer) {
    throw new Error('Free agent not found');
  }

  if (!dropPlayer) {
    throw new Error('Player to waive not found');
  }

  // 2. Validation
  if (addPlayer.team !== 'FA') {
    throw new Error('Selected player is not a free agent');
  }

  if (dropPlayer.team !== team) {
    throw new Error('Player to waive does not belong to this team');
  }

  // 3. Find row indexes in sheet
  const [, ...dataRows] = rows;

  const addRowIndex = dataRows.findIndex(
    row =>
      row[2] === addPlayer.first &&
      row[3] === addPlayer.last &&
      Number(row[5]) === addPlayer.age
  );

  const dropRowIndex = dataRows.findIndex(
    row =>
      row[2] === dropPlayer.first &&
      row[3] === dropPlayer.last &&
      Number(row[5]) === dropPlayer.age
  );

  if (addRowIndex === -1 || dropRowIndex === -1) {
    throw new Error('Player row not found in sheet');
  }

  // Sheet row numbers (account for header row)
  const addSheetRow = addRowIndex + 2;
  const dropSheetRow = dropRowIndex + 2;

  // 4. Execute updates
  await sheets.spreadsheets.values.batchUpdate({
    spreadsheetId: SHEET_ID,
    requestBody: {
      valueInputOption: 'RAW',
      data: [
        {
          range: `Players!A${addSheetRow}`,
          values: [[team]],
        },
        {
          range: `Players!A${dropSheetRow}`,
          values: [['FA']],
        },
      ],
    },
  });

  return { success: true };
}
</file>

<file path="lib/transactionsts.txt">
import { sheets, SHEET_ID } from './googleSheets';

type Transaction = {
  type: string;
  identity: string;
  fromTeam?: string;
  toTeam?: string;
  coach: string;
  details?: string;
  status?: string;
  weekBack?: string;
};

/**
 * Updates the 'Current Owner' in the DraftPicks sheet when a pick is traded.
 */
async function updatePickOwner(details: string, newOwnerShort: string, fromTeamShort: string) {
  try {
    const response = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'DraftPicks!A:F',
    });
    const rows = response.data.values || [];

    const match = details.match(/(\d{4})\s+Round\s+(\d+)/i);
    if (!match) return;

    const [_, year, round] = match;

    // FIX: Look for Year, Round, AND the current owner (Column E/Index 4)
    // This prevents accidentally grabbing the #1 overall pick every time.
    const rowIndex = rows.findIndex(row => 
      row[0] === year && 
      row[1] === round && 
      row[4]?.toUpperCase() === fromTeamShort.toUpperCase()
    );

    if (rowIndex !== -1) {
      const rangeToUpdate = `DraftPicks!E${rowIndex + 1}`; 
      await sheets.spreadsheets.values.update({
        spreadsheetId: SHEET_ID,
        range: rangeToUpdate,
        valueInputOption: 'RAW',
        requestBody: {
          values: [[newOwnerShort.toUpperCase()]],
        },
      });
    } else {
      console.warn(`Could not find a ${year} Round ${round} pick owned by ${fromTeamShort}`);
    }
  } catch (err) {
    console.error("DraftPicks update failed:", err);
  }
}

function capitalize(word: string) {
  if (!word) return '';
  return word.charAt(0).toUpperCase() + word.slice(1).toLowerCase();
}

function formatMessage(identity: string) {
  if (!identity || !identity.includes('|')) return identity;
  const [first, last, , off, def] = identity.split('|');
  const positionRaw = off || def || '';
  const position = positionRaw.toUpperCase();
  const firstName = capitalize(first);
  const lastName = capitalize(last);
  return `${position} - ${firstName} ${lastName}`;
}

function formatTimestamp(date: Date) {
  const mm = String(date.getMonth() + 1).padStart(2, '0');
  const dd = String(date.getDate()).padStart(2, '0');
  const yyyy = date.getFullYear();
  const hh = String(date.getHours()).padStart(2, '0');
  const min = String(date.getMinutes()).padStart(2, '0');
  const ss = String(date.getSeconds()).padStart(2, '0');
  return `${mm}/${dd}/${yyyy} ${hh}:${min}:${ss}`;
}

export async function logTransaction(tx: Transaction) {
  const timestamp = formatTimestamp(new Date());

  // --- 1. MESSAGE LOGIC (THE FIX) ---
  let finalMessage = "";

  if (tx.type === 'TRADE') {
    finalMessage = tx.identity;
  } else if (tx.type === 'INJURY PICKUP' && tx.details) {
    // Use the long custom string (e.g., "G - Logan Bruss for injury to...")
    finalMessage = tx.details;
  } else {
    // For ADD, DROP, or IR: Convert "first|last|..." to "POS - First Last"
    finalMessage = formatMessage(tx.identity);
  }

  // --- 2. TEAM NAME LOOKUP ---
  let fullFrom = tx.fromTeam || 'FA';
  let fullTo = tx.toTeam || 'FA';
  
  try {
    const configRes = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Config!A:B', 
    });
    const configRows = configRes.data.values || [];
    const teamMap = new Map(configRows.map(row => [row[1], row[0]]));
    
    if (tx.fromTeam && teamMap.has(tx.fromTeam)) fullFrom = teamMap.get(tx.fromTeam)!;
    if (tx.toTeam && teamMap.has(tx.toTeam)) fullTo = teamMap.get(tx.toTeam)!;
  } catch (err) {
    console.error("Team name lookup failed:", err);
  }

  if (tx.type === 'TRADE' && tx.identity.toLowerCase().includes('round')) {
    await updatePickOwner(tx.identity, tx.toTeam || '', tx.fromTeam || '');
  }

  // --- 3. APPEND TO TRANSACTION LOG ---
  // Maps to: A:Timestamp, B:Type, C:Desc, D:fromFull, E:toFull, F:fromShort, G:toShort, H:Owner, I:Status, J:WeekBack
  await sheets.spreadsheets.values.append({
    spreadsheetId: SHEET_ID,
    range: 'Transactions!A:J', 
    valueInputOption: 'RAW',
    requestBody: {
      values: [[
        timestamp,
        tx.type,
        finalMessage,             // Column C: The Description
        fullFrom,                 // Column D: Full Team From
        fullTo,                   // Column E: Full Team To
        tx.fromTeam || 'FA',      // Column F: Short Team From
        tx.toTeam || 'FA',        // Column G: Short Team To
        tx.coach,                 // Column H: Owner (Coach Name)
        tx.status || 'INSTANT',   // Column I: Status
        tx.weekBack || ''         // Column J: Week Back
      ]],
    },
  });
}
</file>

<file path="lib/utils.ts">
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
</file>

<file path="postcss.config.mjs">
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
</file>

<file path="public/file.svg">
<svg fill="none" viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg"><path d="M14.5 13.5V5.41a1 1 0 0 0-.3-.7L9.8.29A1 1 0 0 0 9.08 0H1.5v13.5A2.5 2.5 0 0 0 4 16h8a2.5 2.5 0 0 0 2.5-2.5m-1.5 0v-7H8v-5H3v12a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1M9.5 5V2.12L12.38 5zM5.13 5h-.62v1.25h2.12V5zm-.62 3h7.12v1.25H4.5zm.62 3h-.62v1.25h7.12V11z" clip-rule="evenodd" fill="#666" fill-rule="evenodd"/></svg>
</file>

<file path="public/globe.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><g clip-path="url(#a)"><path fill-rule="evenodd" clip-rule="evenodd" d="M10.27 14.1a6.5 6.5 0 0 0 3.67-3.45q-1.24.21-2.7.34-.31 1.83-.97 3.1M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16m.48-1.52a7 7 0 0 1-.96 0H7.5a4 4 0 0 1-.84-1.32q-.38-.89-.63-2.08a40 40 0 0 0 3.92 0q-.25 1.2-.63 2.08a4 4 0 0 1-.84 1.31zm2.94-4.76q1.66-.15 2.95-.43a7 7 0 0 0 0-2.58q-1.3-.27-2.95-.43a18 18 0 0 1 0 3.44m-1.27-3.54a17 17 0 0 1 0 3.64 39 39 0 0 1-4.3 0 17 17 0 0 1 0-3.64 39 39 0 0 1 4.3 0m1.1-1.17q1.45.13 2.69.34a6.5 6.5 0 0 0-3.67-3.44q.65 1.26.98 3.1M8.48 1.5l.01.02q.41.37.84 1.31.38.89.63 2.08a40 40 0 0 0-3.92 0q.25-1.2.63-2.08a4 4 0 0 1 .85-1.32 7 7 0 0 1 .96 0m-2.75.4a6.5 6.5 0 0 0-3.67 3.44 29 29 0 0 1 2.7-.34q.31-1.83.97-3.1M4.58 6.28q-1.66.16-2.95.43a7 7 0 0 0 0 2.58q1.3.27 2.95.43a18 18 0 0 1 0-3.44m.17 4.71q-1.45-.12-2.69-.34a6.5 6.5 0 0 0 3.67 3.44q-.65-1.27-.98-3.1" fill="#666"/></g><defs><clipPath id="a"><path fill="#fff" d="M0 0h16v16H0z"/></clipPath></defs></svg>
</file>

<file path="public/next.svg">
<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 394 80"><path fill="#000" d="M262 0h68.5v12.7h-27.2v66.6h-13.6V12.7H262V0ZM149 0v12.7H94v20.4h44.3v12.6H94v21h55v12.6H80.5V0h68.7zm34.3 0h-17.8l63.8 79.4h17.9l-32-39.7 32-39.6h-17.9l-23 28.6-23-28.6zm18.3 56.7-9-11-27.1 33.7h17.8l18.3-22.7z"/><path fill="#000" d="M81 79.3 17 0H0v79.3h13.6V17l50.2 62.3H81Zm252.6-.4c-1 0-1.8-.4-2.5-1s-1.1-1.6-1.1-2.6.3-1.8 1-2.5 1.6-1 2.6-1 1.8.3 2.5 1a3.4 3.4 0 0 1 .6 4.3 3.7 3.7 0 0 1-3 1.8zm23.2-33.5h6v23.3c0 2.1-.4 4-1.3 5.5a9.1 9.1 0 0 1-3.8 3.5c-1.6.8-3.5 1.3-5.7 1.3-2 0-3.7-.4-5.3-1s-2.8-1.8-3.7-3.2c-.9-1.3-1.4-3-1.4-5h6c.1.8.3 1.6.7 2.2s1 1.2 1.6 1.5c.7.4 1.5.5 2.4.5 1 0 1.8-.2 2.4-.6a4 4 0 0 0 1.6-1.8c.3-.8.5-1.8.5-3V45.5zm30.9 9.1a4.4 4.4 0 0 0-2-3.3 7.5 7.5 0 0 0-4.3-1.1c-1.3 0-2.4.2-3.3.5-.9.4-1.6 1-2 1.6a3.5 3.5 0 0 0-.3 4c.3.5.7.9 1.3 1.2l1.8 1 2 .5 3.2.8c1.3.3 2.5.7 3.7 1.2a13 13 0 0 1 3.2 1.8 8.1 8.1 0 0 1 3 6.5c0 2-.5 3.7-1.5 5.1a10 10 0 0 1-4.4 3.5c-1.8.8-4.1 1.2-6.8 1.2-2.6 0-4.9-.4-6.8-1.2-2-.8-3.4-2-4.5-3.5a10 10 0 0 1-1.7-5.6h6a5 5 0 0 0 3.5 4.6c1 .4 2.2.6 3.4.6 1.3 0 2.5-.2 3.5-.6 1-.4 1.8-1 2.4-1.7a4 4 0 0 0 .8-2.4c0-.9-.2-1.6-.7-2.2a11 11 0 0 0-2.1-1.4l-3.2-1-3.8-1c-2.8-.7-5-1.7-6.6-3.2a7.2 7.2 0 0 1-2.4-5.7 8 8 0 0 1 1.7-5 10 10 0 0 1 4.3-3.5c2-.8 4-1.2 6.4-1.2 2.3 0 4.4.4 6.2 1.2 1.8.8 3.2 2 4.3 3.4 1 1.4 1.5 3 1.5 5h-5.8z"/></svg>
</file>

<file path="public/vercel.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1155 1000"><path d="m577.3 0 577.4 1000H0z" fill="#fff"/></svg>
</file>

<file path="public/window.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><path fill-rule="evenodd" clip-rule="evenodd" d="M1.5 2.5h13v10a1 1 0 0 1-1 1h-11a1 1 0 0 1-1-1zM0 1h16v11.5a2.5 2.5 0 0 1-2.5 2.5h-11A2.5 2.5 0 0 1 0 12.5zm3.75 4.5a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5M7 4.75a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0m1.75.75a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5" fill="#666"/></svg>
</file>

<file path="README.md">
This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `app/page.tsx`. The page auto-updates as you edit the file.

This project uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load [Geist](https://vercel.com/font), a new font family for Vercel.

## Learn More

To learn more about Next.js, take a look at the following resources:

- [Next.js Documentation](https://nextjs.org/docs) - learn about Next.js features and API.
- [Learn Next.js](https://nextjs.org/learn) - an interactive Next.js tutorial.

You can check out [the Next.js GitHub repository](https://github.com/vercel/next.js) - your feedback and contributions are welcome!

## Deploy on Vercel

The easiest way to deploy your Next.js app is to use the [Vercel Platform](https://vercel.com/new?utm_medium=default-template&filter=next.js&utm_source=create-next-app&utm_campaign=create-next-app-readme) from the creators of Next.js.

Check out our [Next.js deployment documentation](https://nextjs.org/docs/app/building-your-application/deploying) for more details.
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}
</file>

<file path=".gitignore">
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
</file>

<file path="app/api/draft-selection/route.ts">
import { NextRequest, NextResponse } from 'next/server';
import { sheets, SHEET_ID } from '@/lib/googleSheets';

export async function POST(req: NextRequest) {
  try {
    const { overallPick, playerIdentity, playerName, playerPosition, newOwnerCode } = await req.json();

    // Generate Timestamp
    const timestamp = new Date().toLocaleString('en-US', {
      timeZone: 'America/New_York', month: '2-digit', day: '2-digit', year: 'numeric',
      hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: false
    }).replace(',', '');

    // 1. Find Row in DraftPicks (Search Column C)
    const picksRes = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'DraftPicks!C:C',
    });
    const rows = picksRes.data.values || [];
    const rowIndex = rows.findIndex(row => row[0] === String(overallPick)) + 1;

    if (rowIndex === 0) throw new Error('Pick not found');

    const draftString = `${playerPosition} - ${playerName}`;

    // 2. Update Status (F), Draft Pick (G), and Datetime (H)
    await sheets.spreadsheets.values.update({
      spreadsheetId: SHEET_ID,
      range: `DraftPicks!F${rowIndex}:H${rowIndex}`,
      valueInputOption: 'USER_ENTERED',
      requestBody: { values: [['Completed', draftString, timestamp]] },
    });

    // 3. Update Players Sheet (Assign team)
    // Inside your POST function in /api/draft-selection/route.ts

// 1. Fetch the Players sheet
const playersRes = await sheets.spreadsheets.values.get({
  spreadsheetId: SHEET_ID,
  range: 'Players!A:I', // Ensure we get enough columns
});
const playerRows = playersRes.data.values || [];

// 2. Parse the identity string (e.g., "John|Doe|24|QB||")
const p = playerIdentity.toLowerCase().split('|');

const playerRowIndex = playerRows.findIndex((r, index) => {
  if (index === 0) return false; // Skip header row

  const match = (sheetVal: any, identityVal: any) => {
    // If identity part is empty, we consider it a match for that specific attribute
    if (!identityVal || identityVal.trim() === '') return true;
    return String(sheetVal || '').toLowerCase().trim() === String(identityVal).toLowerCase().trim();
  };

  return (
    match(r[2], p[0]) && // First Name (Col C)
    match(r[3], p[1]) && // Last Name (Col D)
    match(r[5], p[2]) && // Age (Col F)
    match(r[6], p[3]) && // Pos 1 (Col G)
    match(r[7], p[4]) && // Pos 2 (Col H)
    match(r[8], p[5])    // Pos 3 (Col I)
  );
}) + 1; // +1 because findIndex is 0-based and Sheets is 1-based

if (playerRowIndex > 0) {
  console.log(`Match found at row ${playerRowIndex}. Updating team to ${newOwnerCode}`);
  
  // 3. Update Column A (Team Owner)
  await sheets.spreadsheets.values.update({
    spreadsheetId: SHEET_ID,
    range: `Players!A${playerRowIndex}`, 
    valueInputOption: 'USER_ENTERED',
    requestBody: { values: [[newOwnerCode]] },
  });
} else {
  console.error("No player match found for identity:", playerIdentity);
}

    return NextResponse.json({ success: true });
  } catch (error: any) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }
}
</file>

<file path="app/api/free-agents/route.ts">
import { NextResponse } from 'next/server';
import { sheets, SHEET_ID } from '../../../lib/googleSheets';
import { parsePlayers } from '../../../lib/players';
import { executeFreeAgentMove } from '../../../lib/freeAgency';

export async function GET() {
  const result = await sheets.spreadsheets.values.get({
    spreadsheetId: SHEET_ID,
    range: 'Players',
  });

  const players = parsePlayers(result.data.values || []);
  const freeAgents = players.filter(p => p.team === 'FA');

  return NextResponse.json(freeAgents);
}

export async function POST(req: Request) {
  try {
    const body = await req.json();
    const { team, addIdentity, dropIdentity } = body;

    if (!team || !addIdentity || !dropIdentity) {
      return NextResponse.json(
        { error: 'Missing required fields' },
        { status: 400 }
      );
    }

    await executeFreeAgentMove(team, addIdentity, dropIdentity);

    return NextResponse.json({ success: true });
  } catch (err: any) {
    console.error('Free agency error:', err);
    return NextResponse.json(
      { error: err.message },
      { status: 500 }
    );
  }
}
</file>

<file path="app/api/players/route.ts">
import { NextRequest, NextResponse } from 'next/server';
import { sheets, SHEET_ID } from '../../../lib/googleSheets';

interface Player {
  team: string;
  originalTeam: string;
  first: string;
  last: string;
  nickname?: string;
  age?: number;
  offense?: string;
  defense?: string;
  special?: string;
  position: string;
  identity: string; // Add this field
}

export async function GET(req: NextRequest) {
  const teamShort = req.nextUrl.searchParams.get('team');

  try {
    const result = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Players',
    });

    const rows: string[][] = result.data.values || [];
    const [headerRow, ...dataRows] = rows;

    const headerMap = headerRow.reduce<Record<string, number>>((acc, col, idx) => {
      acc[col] = idx;
      return acc;
    }, {});

    let players: Player[] = dataRows.map((row) => {
      const first = row[headerMap['first']]?.trim() || '';
      const last = row[headerMap['last']]?.trim() || '';
      const age = row[headerMap['age']]?.trim() || '';
      const offense = row[headerMap['offense']]?.trim() || '';
      const defense = row[headerMap['defense']]?.trim() || '';
      const special = row[headerMap['special']]?.trim() || '';

      const positionParts = [offense, defense, special].filter(Boolean);
      const position = positionParts.join('/');

      // Construct the identity string to match the Free Agents format
      // Format: first|last|age|offense|defense|special (lowercase)
      const identity = `${first}|${last}|${age}|${offense}|${defense}|${special}`.toLowerCase();

      return {
        team: row[headerMap['team']],
        originalTeam: row[headerMap['original team']],
        first,
        last,
        nickname: row[headerMap['nickname']] || '',
        age: age ? Number(age) : undefined,
        offense: offense || undefined,
        defense: defense || undefined,
        special: special || undefined,
        position,
        identity, // Return the identity to the frontend
      };
    });

    if (teamShort) {
      players = players.filter((p) => p.team === teamShort);
    }

    return NextResponse.json(players);
  } catch (err) {
    console.error('Failed to fetch players from Google Sheets', err);
    return NextResponse.json({ error: 'Failed to fetch players' }, { status: 500 });
  }
}
</file>

<file path="app/api/teams/route.ts">
import { NextResponse } from 'next/server';
import { sheets, SHEET_ID } from '@/lib/googleSheets';

export async function GET() {
  try {
    const result = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Config',
    });

    const values = result.data.values;

    if (!values || values.length === 0) {
      return NextResponse.json([], { status: 200 });
    }

    const [header, ...rows] = values;

    const index: Record<string, number> = Object.fromEntries(
      header.map((h: string, i: number) => [h.toLowerCase(), i])
    );

    const teams = rows.map((row) => ({
      name: row[index.team] ?? '',
      short: row[index.teamshort] ?? '',
      coach: row[index.coach] ?? '',
      commissioner: row[index.commissioner] === 'TRUE',
    }));

    return NextResponse.json(teams);
  } catch (error) {
    console.error('API /teams failed:', error);

    return NextResponse.json(
      { error: 'Failed to load teams' },
      { status: 500 }
    );
  }
}
</file>

<file path="app/rules/page.tsx">
//need to save Constition from google doc as md file via file->download menu to update content


import fs from 'fs';
import path from 'path';
import { remark } from 'remark';
import html from 'remark-html';
import toc from 'remark-toc';
import slug from 'rehype-slug';

export default async function RulesPage() {
  //const fullPath = path.join(process.cwd(), 'app/content/constitution.md');
  const fullPath = path.resolve(process.cwd(), 'app', 'content', 'constitution.md');
  const fileContents = fs.readFileSync(fullPath, 'utf8');

  // Convert Markdown to HTML with TOC and Slugs (for anchor links)
  const processedContent = await remark()
    .use(toc, { heading: 'contents|table of contents|toc' }) // Looks for a "# Contents" header
    .use(html)
    .process(fileContents);
    
  const contentHtml = processedContent.toString();

  return (
    <div className="max-w-7xl mx-auto p-6 md:p-12 min-h-screen bg-gray-50">
      <div className="flex flex-col lg:flex-row gap-12">
        
        {/* LEFT COLUMN: Sticky Navigation */}
        <aside className="lg:w-1/4">
          <div className="sticky top-8 bg-white p-6 rounded-2xl shadow-sm border border-slate-200">
            <h3 className="text-xs font-black text-blue-600 uppercase tracking-widest mb-4">
              Constitution Sections
            </h3>
            <nav className="space-y-2 text-sm font-bold text-slate-600">
              {/* These can be hardcoded or extracted. Let's start simple: */}
              <a href="#article-i-league-structure" className="block hover:text-blue-600">I. League Structure</a>
              <a href="#article-ii-roster-settings" className="block hover:text-blue-600">II. Roster Settings</a>
              <a href="#article-iii-scoring" className="block hover:text-blue-600">III. Scoring</a>
              <a href="#article-iv-drafting" className="block hover:text-blue-600">IV. Drafting</a>
              <a href="#article-v-trades" className="block hover:text-blue-600">V. Trades</a>
            </nav>
          </div>
        </aside>

        {/* RIGHT COLUMN: The Rules */}
        <main className="lg:w-3/4 bg-white p-8 md:p-16 rounded-3xl shadow-sm border border-slate-100">
          <div className="mb-12 border-b border-slate-100 pb-8">
            <h1 className="text-5xl font-black text-slate-900 tracking-tighter uppercase">
              League Constitution
            </h1>
            <p className="text-slate-400 font-mono text-xs mt-4 uppercase tracking-widest">
              Standard Operating Procedure • 2026 Edition
            </p>
          </div>

          <article 
            className="prose prose-slate prose-headings:font-black prose-h2:text-2xl prose-h2:border-b prose-h2:pb-2 prose-a:text-blue-600 max-w-none prose-img:rounded-xl"
            dangerouslySetInnerHTML={{ __html: contentHtml }} 
          />
        </main>
      </div>
    </div>
  );
}
</file>

<file path="app/transactions/components/DropPlayer.tsx">
'use client';

import { useEffect, useState, useMemo } from 'react';

export default function DropPlayer({ team, coach, onComplete }: { team: string; coach: string; onComplete?: () => void }) {
  const [roster, setRoster] = useState<any[]>([]);
  const [selectedIdentity, setSelectedIdentity] = useState('');
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    if (team) {
      fetch('/api/players')
        .then(res => res.json())
        .then(data => {
          const players = Array.isArray(data) ? data.filter((p: any) => p.teamShort === team || p.team === team) : [];
          setRoster(players);
        })
        .catch(console.error);
    }
  }, [team]);

  // Sort roster by Last Name
  const sortedRoster = useMemo(() => {
    return [...roster].sort((a, b) => {
      const lastA = (a.last || a.name || "").toLowerCase();
      const lastB = (b.last || b.name || "").toLowerCase();
      return lastA.localeCompare(lastB);
    });
  }, [roster]);

  async function handleDrop() {
    if (!selectedIdentity || !team) return;
    setLoading(true);

    try {
      const res = await fetch('/api/transactions', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          timestamp: new Date().toLocaleString(),
          type: 'DROP',
          identity: selectedIdentity,
          fromTeam: team,
          fromShort: team,
          coach,
          details: `Dropped/Waived: ${selectedIdentity}`,
          status: 'PENDING'
        }),
      });

      if (res.ok) {
        alert('Player dropped successfully');
        setRoster(prev => prev.filter(p => p.identity !== selectedIdentity));
        setSelectedIdentity('');
        if (onComplete) onComplete();
      }
    } catch (err) {
      alert('Error dropping player');
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="space-y-4 border p-4 rounded bg-white shadow-sm border-red-200 text-left">
      <h3 className="font-bold text-lg uppercase text-red-600">Waive Player</h3>
      <select
        value={selectedIdentity}
        onChange={e => setSelectedIdentity(e.target.value)}
        className="border p-2 w-full rounded outline-none focus:ring-2 focus:ring-red-500 text-black"
      >
        <option value="">-- Select Player to Drop --</option>
        {sortedRoster.map((p, i) => (
          <option key={i} value={p.identity}>
            {p.last || p.name}, {p.first || ''} ({p.position || p.pos})
          </option>
        ))}
      </select>
      <button
        onClick={handleDrop}
        disabled={loading || !selectedIdentity}
        className={`w-full p-3 rounded font-bold text-white ${loading || !selectedIdentity ? 'bg-gray-300' : 'bg-red-600 hover:bg-red-700'}`}
      >
        {loading ? 'Processing...' : 'Confirm Waive'}
      </button>
    </div>
  );
}
</file>

<file path="app/transactions/components/FreeAgentPanel.tsx">
'use client';

import { useEffect, useState, useMemo } from 'react';

export default function FreeAgentPanel({ team, coach, onComplete }: { team: string; coach: string; onComplete?: () => void }) {
  const [freeAgents, setFreeAgents] = useState<any[]>([]);
  const [myRoster, setMyRoster] = useState<any[]>([]);
  const [selectedIdentity, setSelectedIdentity] = useState('');
  const [loading, setLoading] = useState(false);

  const [isInjuryMode, setIsInjuryMode] = useState(false);
  const [injuredIdentity, setInjuredIdentity] = useState('');
  const [weekOccurred, setWeekOccurred] = useState('');
  const [duration, setDuration] = useState('');

  const resolveCode = (teamString: string) => {
    if (!teamString) return "";
    const match = teamString.match(/\(([^)]+)\)/);
    return (match ? match[1] : teamString).trim().toUpperCase();
  };

  const activeCode = useMemo(() => resolveCode(team), [team]);

  useEffect(() => {
    const loadData = async () => {
      try {
        const [faRes, playersRes] = await Promise.all([
          fetch('/api/free-agents').then(res => res.json()),
          fetch('/api/players').then(res => res.json())
        ]);
        setFreeAgents(Array.isArray(faRes) ? faRes : []);
        if (activeCode) {
          const filtered = Array.isArray(playersRes) 
            ? playersRes.filter((p: any) => resolveCode(p.team) === activeCode) 
            : [];
          setMyRoster(filtered);
        }
      } catch (err) { console.error(err); }
    };
    loadData();
  }, [activeCode]);

  const sortedFreeAgents = useMemo(() => [...freeAgents].sort((a, b) => a.last.toLowerCase().localeCompare(b.last.toLowerCase())), [freeAgents]);
  const sortedMyRoster = useMemo(() => [...myRoster].sort((a, b) => (a.last || a.name || "").toLowerCase().localeCompare((b.last || b.name || "").toLowerCase())), [myRoster]);

  const weekBack = (Number(weekOccurred) || 0) + (Number(duration) || 0);

  // --- LOG PREVIEW LOGIC ---
  const logPreview = useMemo(() => {
    if (!selectedIdentity) return "";
    
    const fa = freeAgents.find(p => p.identity === selectedIdentity);
    const faName = fa ? `${fa.first} ${fa.last}` : "Player";
    const faPos = fa?.position || "??";

    if (!isInjuryMode) return `${faPos} - ${faName}`;

    const injured = myRoster.find(p => p.identity === injuredIdentity);
    const injuredName = injured ? (injured.name || `${injured.first} ${injured.last}`) : "___";
    const injuredPos = injured?.position || "??";

    // FORMAT: G - Logan Bruss for injury to G - Jon Smith in week 1 for 3 weeks
    return `${faPos} - ${faName} for injury to ${injuredPos} - ${injuredName} in week ${weekOccurred || 'X'} for ${duration || 'Y'} weeks`;
  }, [selectedIdentity, isInjuryMode, injuredIdentity, weekOccurred, duration, freeAgents, myRoster]);

  async function handleAdd() {
    if (!selectedIdentity) return alert("Select a player.");
    if (isInjuryMode && (!injuredIdentity || !weekOccurred || !duration)) {
      return alert("Complete injury details first.");
    }

    setLoading(true);

    const payload = {
      type: isInjuryMode ? 'INJURY PICKUP' : 'ADD',
      identity: selectedIdentity,
      fromTeam: 'Free Agent',
      toTeam: team,
      coach: coach,
      details: logPreview, // Send the exact string from the preview
      weekBack: isInjuryMode ? String(weekBack) : '',
      status: isInjuryMode ? 'PENDING' : 'INSTANT'
    };

    try {
      const res = await fetch('/api/transactions', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      });

      if (res.ok) {
        alert('Transaction Logged!');
        setFreeAgents(prev => prev.filter(p => p.identity !== selectedIdentity));
        setSelectedIdentity('');
        setInjuredIdentity('');
        setWeekOccurred('');
        setDuration('');
        setIsInjuryMode(false);
        if (onComplete) onComplete();
      }
    } catch (err) { alert('Error'); } finally { setLoading(false); }
  }

  return (
    <div className="space-y-4 border p-5 rounded-xl bg-white shadow-lg border-blue-100 text-left">
      <div className="flex justify-between items-center border-b pb-3">
        <h3 className="font-black text-xl uppercase text-blue-700 tracking-tighter">Free Agent Pickup</h3>
        <label className="flex items-center gap-2 text-[10px] font-black text-amber-600 cursor-pointer uppercase">
          <input type="checkbox" checked={isInjuryMode} onChange={(e) => setIsInjuryMode(e.target.checked)} />
          Injury Replacement?
        </label>
      </div>

      <div className="space-y-1">
        <label className="text-[10px] font-bold text-gray-400 uppercase">Available Free Agents</label>
        <select value={selectedIdentity} onChange={e => setSelectedIdentity(e.target.value)} className="border-2 border-gray-100 p-3 w-full rounded-lg text-sm text-black">
          <option value="">-- Choose Player --</option>
          {sortedFreeAgents.map((p, i) => <option key={i} value={p.identity}>{p.last}, {p.first} ({p.position})</option>)}
        </select>
      </div>

      {isInjuryMode && (
        <div className="p-4 bg-amber-50 border border-amber-200 rounded-xl space-y-4">
          <label className="text-[10px] font-black text-amber-700 uppercase">Injured Player ({activeCode})</label>
          <select value={injuredIdentity} onChange={e => setInjuredIdentity(e.target.value)} className="border-2 border-white p-2 w-full rounded-lg text-sm bg-white text-black">
            <option value="">-- Select Player --</option>
            {sortedMyRoster.map((p, i) => <option key={i} value={p.identity}>{p.last || p.name}, {p.first || ''} ({p.position})</option>)}
          </select>
          <div className="grid grid-cols-2 gap-4">
            <input type="number" placeholder="Week" value={weekOccurred} onChange={e => setWeekOccurred(e.target.value)} className="p-2 border rounded text-black text-sm" />
            <input type="number" placeholder="Duration" value={duration} onChange={e => setDuration(e.target.value)} className="p-2 border rounded text-black text-sm" />
          </div>
        </div>
      )}

      {selectedIdentity && (
        <div className="p-3 bg-slate-50 border border-slate-200 rounded-lg">
          <p className="text-[9px] font-bold text-slate-400 uppercase mb-1">Log Entry Preview:</p>
          <p className="text-xs font-mono text-slate-700 italic leading-relaxed">{logPreview}</p>
        </div>
      )}

      <button onClick={handleAdd} disabled={loading || !selectedIdentity} className={`w-full p-4 rounded-xl font-black text-white uppercase tracking-widest ${loading || !selectedIdentity ? 'bg-gray-200' : isInjuryMode ? 'bg-amber-500' : 'bg-blue-600'}`}>
        {loading ? 'Processing...' : 'Submit Pickup'}
      </button>
    </div>
  );
}
</file>

<file path="app/transactions/components/IRPanel.tsx">
'use client';

import { useEffect, useState, useMemo } from 'react';

export default function IRPanel({ team, coach, onComplete }: { team: string; coach: string; onComplete?: () => void }) {
  const [roster, setRoster] = useState<any[]>([]);
  const [selectedIdentity, setSelectedIdentity] = useState('');
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    if (team) {
      fetch('/api/players')
        .then(res => res.json())
        .then(data => {
          const players = Array.isArray(data) ? data.filter((p: any) => p.teamShort === team || p.team === team) : [];
          setRoster(players);
        })
        .catch(console.error);
    }
  }, [team]);

  const sortedRoster = useMemo(() => {
    return [...roster].sort((a, b) => {
      const lastA = (a.last || a.name || "").toLowerCase();
      const lastB = (b.last || b.name || "").toLowerCase();
      return lastA.localeCompare(lastB);
    });
  }, [roster]);

  async function handleIR() {
    if (!selectedIdentity || !team) return;
    setLoading(true);

    try {
      const res = await fetch('/api/transactions', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          timestamp: new Date().toLocaleString(),
          type: 'IR MOVE',
          identity: selectedIdentity,
          toTeam: 'IR',
          fromShort: team,
          coach,
          details: `Placed on IR: ${selectedIdentity}`,
          status: 'PENDING'
        }),
      });

      if (res.ok) {
        alert('Player moved to IR');
        setRoster(prev => prev.filter(p => p.identity !== selectedIdentity));
        setSelectedIdentity('');
        if (onComplete) onComplete();
      }
    } catch (err) {
      alert('Error moving to IR');
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="space-y-4 border p-4 rounded bg-white shadow-sm border-amber-200 text-left">
      <h3 className="font-bold text-lg uppercase text-amber-600">Move to IR</h3>
      <select
        value={selectedIdentity}
        onChange={e => setSelectedIdentity(e.target.value)}
        className="border p-2 w-full rounded outline-none focus:ring-2 focus:ring-amber-500 text-black"
      >
        <option value="">-- Select Player for IR --</option>
        {sortedRoster.map((p, i) => (
          <option key={i} value={p.identity}>
            {p.last || p.name}, {p.first || ''} ({p.position || p.pos})
          </option>
        ))}
      </select>
      <button
        onClick={handleIR}
        disabled={loading || !selectedIdentity}
        className={`w-full p-3 rounded font-bold text-white ${loading || !selectedIdentity ? 'bg-gray-300' : 'bg-amber-600 hover:bg-amber-700'}`}
      >
        {loading ? 'Processing...' : 'Confirm IR Move'}
      </button>
    </div>
  );
}
</file>

<file path="lib/googleSheets.ts">
import { google } from 'googleapis';

const auth = new google.auth.JWT({
  email: process.env.GOOGLE_CLIENT_EMAIL,
  key: process.env.GOOGLE_PRIVATE_KEY?.replace(/\\n/g, '\n'),
  scopes: ['https://www.googleapis.com/auth/spreadsheets'],
});

export const sheets = google.sheets({
  version: 'v4',
  auth,
});

export const SHEET_ID = process.env.GOOGLE_SHEET_ID!;

if (
  !process.env.GOOGLE_CLIENT_EMAIL ||
  !process.env.GOOGLE_PRIVATE_KEY ||
  !process.env.GOOGLE_SHEET_ID
) {
  throw new Error('Missing Google Sheets environment variables');
}
</file>

<file path="lib/playerLookup.ts">
export function findPlayerRowIndex(
  rows: string[][],
  target: { identity: string }
): number {
  const [, ...data] = rows; // skip header

  const foundIndex = data.findIndex((row) => {
    const identity = [
      row[2],               // first
      row[3],               // last
      Number(row[5]),       // age
      row[6] || '',         // offense
      row[7] || '',         // defense
      row[8] || '',         // special
    ]
      .join('|')
      .toLowerCase();

    return identity === target.identity;
  });

  if (foundIndex === -1) {
    throw new Error('Player row index not found');
  }

  // +2 = header row + 1-based sheet index
  return foundIndex + 2;
}
</file>

<file path="next.config.ts">
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
</file>

<file path="app/api/cuts/config/route.ts">
import { NextRequest, NextResponse } from 'next/server';
import { sheets, SHEET_ID } from '@/lib/googleSheets';

export async function GET() {
  try {
    // Expanded range to G20 just to be safe for future config additions
    const response = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Config!F2:G20',
    });

    const rows = response.data.values || [];

    // Transform rows into a key-value object
    const config = rows.reduce((acc, [key, val]) => {
      if (key) acc[key.trim().toLowerCase()] = String(val).trim();
      return acc;
    }, {} as Record<string, string>);

    return NextResponse.json({
      cuts_year: config.cuts_year || '2025',
      draft_year: config.draft_year || '2026',
      protected: parseInt(config.limit_protected) || 30,
      pullback: parseInt(config.limit_pullback) || 8,
      // Add the new date field here
      cuts_due_date: config.cuts_due_date || '', 
    });
  } catch (error) {
    console.error('Config API Error:', error);
    return NextResponse.json({ 
      cuts_year: '2025', 
      draft_year: '2026', 
      protected: 30, 
      pullback: 8,
      cuts_due_date: ''
    });
  }
}
</file>

<file path="app/layout.tsx">
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar"; // Ensure this path matches your Navbar location

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GFL League Manager",
  description: "Manage rosters and trades",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-gray-50`}
      >
        {/* The Navbar shows on every page */}
        <Navbar />
        
        {/* Main content area */}
        <main className="min-h-screen">
          {children}
        </main>
      </body>
    </html>
  );
}
</file>

<file path="components/SelectionModal.tsx">
// components/SelectionModal.tsx
'use client';

import React, { useState, useEffect } from 'react';
import { useRouter } from 'next/navigation';

interface Player {
  identity: string;
  first: string;
  last: string;
  position: string;
  team: string; 
  age?: number; // Added age to support scouting identity
}

interface DraftPick {
  overall: number;
  year: number;
  round: number;
  currentOwner: string;
  currentOwnerCode: string;
  originalTeam: string;
  status: string;
  draftedPlayer: string;
  timestamp: string;
}

interface SelectionModalProps {
  pick: any;
  onClose: () => void;
  onComplete: () => void;
  onScout?: (player: any) => void; // <--- Add this line (the '?' makes it optional)
}

export default function SelectionModal({ pick, onClose, onComplete, onScout }: SelectionModalProps) {
  const router = useRouter();
  const [players, setPlayers] = useState<Player[]>([]);
  const [searchTerm, setSearchTerm] = useState('');
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [loadingPlayers, setLoadingPlayers] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    fetch('/api/players')
      .then(res => res.json())
      .then(data => {
        setPlayers(data.filter((p: Player) => p.team.trim().toUpperCase() === 'FA'));
        setLoadingPlayers(false);
      })
      .catch(err => {
        console.error("Failed to load free agents:", err);
        setError("Failed to load free agents.");
        setLoadingPlayers(false);
      });
  }, []);

  const filteredPlayers = players.filter(p =>
    `${p.first} ${p.last}`.toLowerCase().includes(searchTerm.toLowerCase()) ||
    p.position.toLowerCase().includes(searchTerm.toLowerCase())
  );

  const handleSelect = async (player: Player) => {
    setIsSubmitting(true);
    setError(null);
    try {
      const res = await fetch('/api/draft-selection', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          overallPick: pick.overall,
          playerIdentity: player.identity,
          playerName: `${player.first} ${player.last}`,
          playerPosition: player.position,
          newOwnerCode: pick.currentOwnerCode
        }),
      });
      if (!res.ok) throw new Error('Failed to draft player');
      onComplete(); 
      router.refresh();
    } catch (err: any) {
      setError(err.message);
    } finally {
      setIsSubmitting(false);
    }
  };

  return (
    <div className="fixed inset-0 bg-slate-900/90 backdrop-blur-md z-50 flex items-center justify-center p-4">
      <div className="bg-white rounded-2xl w-full max-w-md shadow-2xl border border-white/20 overflow-hidden">
        {/* MODAL HEADER */}
        <div className="bg-blue-600 p-6 text-white">
          <div className="flex justify-between items-center mb-2">
             <span className="text-[10px] font-black uppercase tracking-widest bg-blue-500 px-2 py-1 rounded-md">Pick #{pick.overall}</span>
             <button onClick={onClose} className="text-white/70 hover:text-white transition-colors text-xl">✕</button>
          </div>
          <h2 className="text-2xl font-black italic uppercase tracking-tighter">Draft Selection</h2>
          <p className="text-blue-100 text-xs font-bold uppercase tracking-widest">On the Clock: {pick.currentOwner}</p>
        </div>

        {/* SEARCH & LIST */}
        <div className="p-6 space-y-4">
          <input 
            type="text"
            placeholder="Search Free Agents..."
            className="w-full p-4 bg-slate-50 border-2 border-slate-100 rounded-xl focus:border-blue-500 outline-none font-bold text-slate-800 transition-all placeholder-slate-400"
            value={searchTerm}
            onChange={(e) => setSearchTerm(e.target.value)}
            autoFocus
            disabled={isSubmitting || loadingPlayers}
          />

          <div className="max-h-80 overflow-y-auto rounded-xl border border-slate-100 divide-y divide-slate-50">
            {loadingPlayers ? (
               <div className="p-10 text-center text-blue-600 animate-pulse">Loading...</div>
            ) : filteredPlayers.map(p => (
              <div key={p.identity} className="p-4 flex justify-between items-center hover:bg-slate-50 transition-colors">
                <div className="flex flex-col">
                  {/* --- HYPERLINK ON NAME --- */}
                  <a 
                    href={`https://www.google.com/search?q=${encodeURIComponent(p.first + ' ' + p.last + ' NFL Scouting')}`}
                    target="_blank" 
                    rel="noopener noreferrer"
                    className="font-black text-slate-800 uppercase tracking-tight hover:text-blue-600 hover:underline decoration-2 underline-offset-4"
                  >
                    {p.first} {p.last}
                  </a>
                  <div className="text-[10px] font-bold text-slate-400 uppercase">{p.position}</div>
                </div>
                
                <div className="flex items-center gap-2">
                  {/* --- SCOUT/DETAILS BUTTON --- */}
                  <button 
                    onClick={() => onScout?.(p)}
                    className="text-[10px] font-black text-blue-600 uppercase bg-blue-50 px-3 py-2 rounded-lg hover:bg-blue-100 transition-all"
                  >
                    Scout
                  </button>

                  <button 
                    disabled={isSubmitting}
                    onClick={() => handleSelect(p)}
                    className="bg-slate-800 text-white text-[10px] font-black px-3 py-2 rounded-lg hover:bg-blue-600 transition-all disabled:opacity-50"
                  >
                    {isSubmitting ? '...' : 'DRAFT'}
                  </button>
                </div>
              </div>
            ))}
          </div>
          <button onClick={onClose} className="w-full py-2 text-slate-400 font-bold text-xs uppercase tracking-widest">Cancel</button>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="lib/players.ts">
import { sheets, SHEET_ID } from './googleSheets';

export type Player = {
  team: string;
  first: string;
  last: string;
  age: number;
  offense: string;
  defense: string;
  special: string;
  position: string;
  isIR: boolean;
  identity: string;
};

/**
 * Builds a stable identity string for a player.
 * This MUST stay consistent across the app.
 */
export function buildPlayerIdentity(player: Player): string {
  return [
    player.first || '',
    player.last || '',
    player.age ?? '',
    player.offense || '',
    player.defense || '',
    player.special || '',
  ]
    .join('|')
    .toLowerCase();
}

/**
 * Parse players from the Google Sheet "Players" tab
 *
 * Expected columns:
 * 0 team
 * 1 original team (unused here)
 * 2 first
 * 3 last
 * 4 nickname (unused)
 * 5 age
 * 6 offense
 * 7 defense
 * 8 special
 */
export function parsePlayers(rows: string[][]): Player[] {
  const [, ...data] = rows; // skip header row

  return data.map((row, rowIndex) => {
    try {
      const team = row[0] || '';

      const offense = row[6] || '';
      const defense = row[7] || '';
      const special = row[8] || '';

      const position = [offense, defense, special].filter(Boolean).join('/');

      const player: Player = {
        team,
        first: row[2] || '',
        last: row[3] || '',
        age: Number(row[5]) || 0,
        offense,
        defense,
        special,
        position,
        isIR: team.includes('-IR'),
        identity: '', // will set next
      };

      return {
        ...player,
        identity: buildPlayerIdentity(player),
      };
    } catch (err) {
      console.error(`Error parsing row ${rowIndex + 2}:`, err);
      return null;
    }
  }).filter(Boolean) as Player[];
}

/**
 * Remove rows in the "Players" sheet where first and last names are both blank.
 */
export async function removeBlankPlayerRows() {
  try {
    const res = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Players',
    });

    const rows = res.data.values || [];

    // Keep header row and rows where first or last name is present
    const filteredRows = rows.filter((row, index) => {
      if (index === 0) return true; // header
      const first = row[2]?.trim();
      const last = row[3]?.trim();
      return first || last;
    });

    if (filteredRows.length === rows.length) {
      console.log('No blank player rows to remove.');
      return;
    }

    // Clear the sheet
    await sheets.spreadsheets.values.clear({
      spreadsheetId: SHEET_ID,
      range: 'Players',
    });

    // Write back filtered rows
    await sheets.spreadsheets.values.update({
      spreadsheetId: SHEET_ID,
      range: 'Players',
      valueInputOption: 'RAW',
      requestBody: {
        values: filteredRows,
      },
    });

    console.log(`Removed ${rows.length - filteredRows.length} blank player rows.`);
  } catch (err) {
    console.error('Error removing blank player rows:', err);
  }
}
</file>

<file path="package.json">
{
  "name": "gfl-fantasy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "-": "^0.0.1",
    "@radix-ui/react-checkbox": "^1.3.3",
    "@radix-ui/react-primitive": "^2.1.4",
    "@radix-ui/react-slot": "^1.2.4",
    "@tailwindcss/typography": "^0.5.19",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "g": "^2.0.1",
    "googleapis": "^169.0.0",
    "lucide-react": "^0.562.0",
    "next": "16.0.10",
    "next-auth": "^4.24.13",
    "react": "19.2.1",
    "react-dom": "19.2.1",
    "rehype-autolink-headings": "^7.1.0",
    "rehype-slug": "^6.0.0",
    "remark": "^15.0.1",
    "remark-html": "^16.0.1",
    "remark-toc": "^9.0.0",
    "tailwind-merge": "^3.4.0",
    "tailwind-variants": "^3.2.2",
    "vercel": "^50.1.3"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10.4.23",
    "eslint": "^9",
    "eslint-config-next": "16.0.10",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18",
    "tw-animate-css": "^1.4.0",
    "typescript": "^5"
  },
  "description": "This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}
</file>

<file path="tailwind.config.js">
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./*.html",
    "./src/**/*.{js,ts,jsx,tsx}"
  ],
  theme: {
    extend: {},
  },
  plugins: [
    require('@tailwindcss/typography'),
  ],
};
</file>

<file path="app/api/rosters/[team]/route.ts">
import { sheets, SHEET_ID } from '@/lib/googleSheets';

type RouteContext = {
  params: Promise<{ team: string }>;
};

export async function GET(req: Request, { params }: RouteContext) {
  try {
    const resolvedParams = await params;
    const teamShort = resolvedParams.team.toUpperCase();

    // Fetch data from both sheets
    const [playersRes, picksRes] = await Promise.all([
      sheets.spreadsheets.values.get({ 
        spreadsheetId: SHEET_ID, 
        range: 'Players!A:I' 
      }),
      sheets.spreadsheets.values.get({ 
        spreadsheetId: SHEET_ID, 
        range: 'DraftPicks!A:G' // Expanded range to A:G for the 7 columns
      })
    ]);

    const allPlayers = playersRes.data.values || [];
    const allPicks = picksRes.data.values || [];

    // 1. Process Players
    const roster = allPlayers
      .filter(row => row[0]?.toUpperCase() === teamShort)
      .map(row => ({
        // Using row[2] and row[3] for First Last name
        name: `${row[2]} ${row[3]}`,
        // ADD THIS LINE: row[5] is Column F (Age)
        age: row[5] || '0',
        // Mapping positions from G, H, I columns
        pos: (row[6] || row[7] || row[8] || '??').toUpperCase(),
        group: row[6] ? 'OFF' : row[7] ? 'DEF' : 'SPEC'
      }));

    // 2. Process Draft Picks
    // Column Index Mapping: 0:Year, 1:Round, 3:Overall, 4:Original Team, 5:Current Owner
    const picks = allPicks
      .filter(row => row[4]?.toUpperCase() === teamShort) // Filter by CURRENT OWNER (Index 5)
      .map(row => ({
        year: row[0],
        round: row[1],
        overall: row[2],
        originalTeam: row[3],
        currentOwner: row[4]
      }))
      // Sort by Year first, then by Overall pick number
      .sort((a, b) => 
        Number(a.year) - Number(b.year) || 
        Number(a.overall) - Number(b.overall)
      );

    return Response.json({ roster, picks });
  } catch (error: any) {
    console.error("Roster API Error:", error);
    return Response.json({ error: error.message }, { status: 500 });
  }
}
</file>

<file path="app/page.tsx">
import Link from 'next/link';

export default function HomePage() {
  const cards = [
    {
      title: 'Team Rosters',
      desc: 'View depth charts, roster sizes, and draft picks.',
      href: '/rosters',
      icon: '📋',
      color: 'border-blue-500'
    },
    {
      title: 'Transactions',
      desc: 'Execute player adds, drops, and team trades.',
      href: '/transactions',
      icon: '🤝',
      color: 'border-green-500'
    },
    {
      title: 'Roster Cuts',
      desc: 'Select your protected and pullback players for the new season.',
      href: '/cuts',
      icon: '✂️',
      color: 'border-amber-500' // New gold color for the offseason tool
    },
    {
      title: 'Draft Board',
      desc: 'Track live selections, available players, and draft order.',
      href: '/draft',
      icon: '🏈',
      color: 'border-purple-500'
    },
    {
      title: 'League Settings',
      desc: 'Manage team names and league rules.',
      href: '#',
      icon: '⚙️',
      color: 'border-gray-500'
    }
  ];

  return (
    <div className="max-w-7xl mx-auto p-8">
      <header className="mb-12">
        <h1 className="text-4xl font-black text-gray-900 tracking-tight text-left uppercase">Front Office</h1>
        <p className="text-gray-500 text-left">Manage your league operations and personnel.</p>
      </header>

      {/* Grid updated to auto-fill to handle 5 cards cleanly */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-6">
        {cards.map((card) => (
          <Link 
            key={card.title} 
            href={card.href}
            className={`block p-6 bg-white border-t-4 ${card.color} rounded-xl shadow-sm hover:shadow-md transition-all hover:-translate-y-1 group text-left`}
          >
            <div className="text-4xl mb-4 group-hover:rotate-12 transition-transform inline-block">
              {card.icon}
            </div>
            <h2 className="text-xl font-bold text-gray-800 uppercase tracking-tight leading-tight">
              {card.title}
            </h2>
            <p className="text-gray-600 mt-2 text-sm leading-relaxed">
              {card.desc}
            </p>
            <div className={`mt-4 text-[10px] font-black opacity-0 group-hover:opacity-100 transition-opacity uppercase tracking-widest ${card.color.replace('border-', 'text-')}`}>
              Launch Tool →
            </div>
          </Link>
        ))}
      </div>
    </div>
  );
}
</file>

<file path="app/transactions/page.tsx">
'use client';

import { useState, useEffect, useMemo } from 'react';
import FreeAgentPanel from './components/FreeAgentPanel';
import DropPlayer from './components/DropPlayer';
import IRPanel from './components/IRPanel';
import TradePanel from './components/TradePanel';

export default function TransactionsPage() {
  const [teams, setTeams] = useState<any[]>([]);
  const [selectedTeam, setSelectedTeam] = useState('');
  const [coach, setCoach] = useState('');
  const [logs, setLogs] = useState<any[]>([]);
  const [loadingLogs, setLoadingLogs] = useState(true);
  const [filterTeam, setFilterTeam] = useState('');

  useEffect(() => {
    fetch('/api/teams').then(res => res.json()).then(setTeams);
  }, []);

  useEffect(() => {
    fetchLogs();
  }, []);

  const fetchLogs = async () => {
    setLoadingLogs(true);
    try {
      const res = await fetch('/api/transactions');
      const data = await res.json();
      setLogs(data);
    } catch (err) {
      console.error("Failed to fetch logs:", err);
    } finally {
      setLoadingLogs(false);
    }
  };

  useEffect(() => {
    const teamObj = teams.find(t => t.short === selectedTeam);
    setCoach(teamObj ? teamObj.coach : '');
  }, [selectedTeam, teams]);

  const filteredLogs = useMemo(() => {
    if (!filterTeam) return logs;
    return logs.filter(log => 
      log.fromShort === filterTeam || log.toShort === filterTeam
    );
  }, [logs, filterTeam]);

  return (
    <div className="max-w-7xl mx-auto p-8 space-y-12">
      <h1 className="text-3xl font-bold border-b pb-4 text-left text-gray-800">GFL Transaction Center</h1>

      {/* GLOBAL SELECTOR */}
      <div className="bg-gray-50 p-6 rounded-xl border border-gray-200 shadow-sm flex flex-col md:flex-row gap-6 items-end">
        <div className="flex-1 w-full text-left">
          <label className="block text-xs font-bold text-gray-500 mb-1 uppercase">Select Active Team</label>
          <select 
            className="border p-3 w-full rounded bg-white shadow-sm focus:ring-2 focus:ring-blue-500 outline-none text-black"
            value={selectedTeam}
            onChange={(e) => setSelectedTeam(e.target.value)}
          >
            <option value="">-- Select Team --</option>
            {teams.map(t => <option key={t.short} value={t.short}>{t.name} ({t.short})</option>)}
          </select>
        </div>
        <div className="flex-1 w-full text-left">
          <label className="block text-xs font-bold text-gray-500 mb-1 uppercase">Coach</label>
          <input className="border p-3 w-full rounded bg-gray-100 text-gray-600 focus:outline-none" value={coach} readOnly />
        </div>
      </div>

      {!selectedTeam ? (
        <div className="text-center py-20 border-2 border-dashed rounded-xl text-gray-400 bg-gray-50">
          <p className="text-xl">Select a team to enable tools.</p>
        </div>
      ) : (
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
          <FreeAgentPanel team={selectedTeam} coach={coach} onComplete={fetchLogs} />
          <DropPlayer team={selectedTeam} coach={coach} />
          <IRPanel team={selectedTeam} coach={coach} />
          <TradePanel team={selectedTeam} coach={coach} />
        </div>
      )}

      {/* TRANSACTION LOG SECTION */}
      <div className="space-y-4">
        <div className="flex justify-between items-center border-b pb-4">
          <h2 className="text-xl font-bold text-gray-700">Recent History</h2>
          <div className="flex gap-3">
            <select 
              className="text-sm border rounded-lg px-3 py-2 bg-white text-black"
              value={filterTeam}
              onChange={(e) => setFilterTeam(e.target.value)}
            >
              <option value="">Show All Teams</option>
              {teams.map(t => <option key={t.short} value={t.short}>{t.name}</option>)}
            </select>
            <button onClick={fetchLogs} className="text-xs font-semibold bg-gray-100 px-4 py-2 rounded-lg border text-black">🔄 Refresh</button>
          </div>
        </div>

        <div className="bg-white border rounded-xl shadow-sm overflow-hidden">
          <div className="overflow-x-auto max-h-[600px] overflow-y-auto">
            <table className="w-full text-left text-sm">
              <thead className="bg-gray-100 sticky top-0 z-10 border-b">
                <tr>
                  <th className="p-4 font-bold text-gray-600 uppercase text-[10px]">Timestamp</th>
                  <th className="p-4 font-bold text-gray-600 uppercase text-[10px]">Type</th>
                  <th className="p-4 font-bold text-gray-600 uppercase text-[10px]">Details</th>
                  <th className="p-4 font-bold text-gray-600 uppercase text-[10px]">From Team</th>
                  <th className="p-4 font-bold text-gray-600 uppercase text-[10px]">To Team</th><th className="p-4 font-bold text-gray-600 uppercase text-[10px]">Owner</th>
                  <th className="p-4 font-bold text-gray-600 uppercase text-[10px]">Week Back</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-gray-100">
                {filteredLogs.map((log, i) => (
                  <tr key={i} className="hover:bg-blue-50/40">
                    <td className="p-4 font-mono text-[11px] text-gray-400">{log.timestamp}</td>
                    <td className="p-4">
                      <span className={`px-2 py-0.5 rounded text-[10px] font-black ${
                        log.type === 'INJURY PICKUP' ? 'bg-amber-100 text-amber-700' : 
                        log.type === 'TRADE' ? 'bg-blue-100 text-blue-700' : 'bg-gray-100 text-gray-700'
                      }`}>
                        {log.type}
                      </span>
                    </td>
                    <td className="p-4 font-medium text-gray-800">{log.details}</td>
                    <td className="p-4 text-gray-600 text-[11px]">{log.fromFull || '-'}</td>
                    <td className="p-4 text-gray-600 text-[11px]">{log.toFull || '-'}</td>
                    <td className="p-4 text-gray-500">{log.coach}</td>
                    <td className="p-4 font-bold text-blue-600">
                      {log.weekBack ? `Week ${log.weekBack}` : '-'}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="app/transactions/components/TradePanel.tsx">
'use client';

import React, { useEffect, useState, useCallback } from 'react';
import { useRouter } from 'next/navigation';

interface Team { name: string; short: string; coach: string; }
interface Player { first: string; last: string; team: string; position: string; identity: string; }
interface DraftPick { year: number; round: number; currentOwner: string; overall: number; originalTeam: string; }

export default function TradePanel({ team, coach }: { team: string; coach: string }) {
  const router = useRouter();
  const [teams, setTeams] = useState<Team[]>([]);
  const [players, setPlayers] = useState<Player[]>([]);
  const [draftPicks, setDraftPicks] = useState<DraftPick[]>([]);

  const [toTeam, setToTeam] = useState('');
  const [fromPlayers, setFromPlayers] = useState<string[]>([]);
  const [toPlayers, setToPlayers] = useState<string[]>([]);
  const [fromDraftPicks, setFromDraftPicks] = useState<string[]>([]);
  const [toDraftPicks, setToDraftPicks] = useState<string[]>([]);

  const [status, setStatus] = useState('');
  const [loading, setLoading] = useState(false);

  const loadData = useCallback(async () => {
    try {
      const [tRes, pRes, dRes] = await Promise.all([
        fetch('/api/teams').then(res => res.json()),
        fetch('/api/players').then(res => res.json()),
        fetch('/api/draft-picks').then(res => res.json())
      ]);
      setTeams(tRes);
      setPlayers(pRes);
      setDraftPicks(dRes);
    } catch (err) {
      console.error("Failed to load trade data:", err);
    }
  }, []);

  useEffect(() => {
    loadData();
  }, [loadData]);

  const resolveCode = (teamString: string) => {
    if (!teamString) return "";
    const match = teamString.match(/\(([^)]+)\)/);
    return (match ? match[1] : teamString).trim().toUpperCase();
  };

  const activeCode = resolveCode(team);
  const partnerCode = resolveCode(toTeam);

  // --- SORTED HELPER LOGIC ---

  // Sort Players by Last Name, then First Name
  const sortPlayers = (playerList: Player[]) => {
    return [...playerList].sort((a, b) => {
      const lastA = (a.last || "").toLowerCase();
      const lastB = (b.last || "").toLowerCase();
      if (lastA !== lastB) return lastA.localeCompare(lastB);
      return (a.first || "").toLowerCase().localeCompare((b.first || "").toLowerCase());
    });
  };

  // Sort Draft Picks by Year (Asc), then Round (Asc)
  const sortPicks = (pickList: DraftPick[]) => {
    return [...pickList].sort((a, b) => {
      if (a.year !== b.year) return a.year - b.year;
      return a.round - b.round;
    });
  };

  const fromTeamPlayers = sortPlayers(players.filter(p => resolveCode(p.team) === activeCode));
  const toTeamPlayers = sortPlayers(players.filter(p => resolveCode(p.team) === partnerCode));
  
  const fromTeamDraftPicks = sortPicks(draftPicks.filter(p => resolveCode(p.currentOwner) === activeCode));
  const toTeamDraftPicks = sortPicks(draftPicks.filter(p => resolveCode(p.currentOwner) === partnerCode));

  const handleTrade = async () => {
    if (!toTeam || (fromPlayers.length === 0 && fromDraftPicks.length === 0 && toPlayers.length === 0 && toDraftPicks.length === 0)) {
      setStatus('⚠️ Please select at least one asset to trade.');
      return;
    }

    setLoading(true);
    setStatus('⏳ Processing trade with Google Sheets...');

    try {
      const formattedPicksFrom = fromDraftPicks.map(id => {
        const p = draftPicks.find(pick => String(pick.overall) === String(id));
        return p ? `Draft Pick ${p.year} Round ${p.round} (#${p.overall})` : id;
      });

      const formattedPicksTo = toDraftPicks.map(id => {
        const p = draftPicks.find(pick => String(pick.overall) === String(id));
        return p ? `Draft Pick ${p.year} Round ${p.round} (#${p.overall})` : id;
      });

      const rawIdentitiesFrom = players
        .filter(p => fromPlayers.includes(p.identity))
        .map(p => p.identity);

      const rawIdentitiesTo = players
        .filter(p => toPlayers.includes(p.identity))
        .map(p => p.identity);

      const res = await fetch('/api/trades', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          fromTeam: activeCode,
          toTeam: partnerCode,
          playersFrom: fromPlayers,
          playersTo: toPlayers,
          draftPicksFrom: formattedPicksFrom, 
          draftPicksTo: formattedPicksTo,     
          rawIdentitiesFrom,        
          rawIdentitiesTo,          
          submittedBy: coach
        }),
      });

      if (res.ok) {
        setStatus('✅ Success! Assets moved and transaction logged.');
        setFromPlayers([]); 
        setToPlayers([]); 
        setFromDraftPicks([]); 
        setToDraftPicks([]); 
        setToTeam('');
        await loadData();
        router.refresh();
      } else {
        const errData = await res.json();
        setStatus(`❌ Trade failed: ${errData.error || 'Server Error'}`);
      }
    } catch (err) {
      setStatus('❌ Network error processing trade.');
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="space-y-4 border p-5 rounded-xl bg-white shadow-lg border-purple-100 text-left transition-all">
      <div className="flex justify-between items-center border-b pb-3">
        <div>
          <h3 className="font-black text-xl uppercase text-purple-700 tracking-tighter">Transaction Center</h3>
          <p className="text-[10px] text-gray-400 font-bold uppercase tracking-widest">Master Sheet Integration</p>
        </div>
        <span className="text-[10px] bg-purple-100 text-purple-700 px-2 py-1 rounded-full font-bold">
          {activeCode} Proposing
        </span>
      </div>

      <div className="space-y-1">
        <label className="text-[10px] font-bold text-gray-400 uppercase tracking-widest block">Select Trade Partner</label>
        <select 
          value={toTeam} 
          onChange={(e) => {
            setToTeam(e.target.value);
            setStatus(''); 
          }} 
          className="border-2 border-gray-100 p-3 w-full rounded-lg text-sm outline-none focus:border-purple-300 transition-colors"
        >
          <option value="">-- Choose Partner Team --</option>
          {teams.filter(t => resolveCode(t.short) !== activeCode).map(t => (
            <option key={t.short} value={t.short}>{t.name}</option>
          ))}
        </select>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {/* SENDER ASSETS */}
        <div className="p-4 bg-blue-50/50 rounded-lg border border-blue-100">
          <p className="text-[11px] font-black text-blue-700 uppercase mb-3 text-center">Your Assets ({activeCode})</p>
          <div className="space-y-3">
            <select 
              multiple 
              className="border-2 border-white w-full h-32 text-xs rounded-lg p-2 bg-white focus:ring-2 focus:ring-blue-200 outline-none" 
              value={fromPlayers} 
              onChange={e => setFromPlayers(Array.from(e.target.selectedOptions, o => o.value))}
            >
              <optgroup label="Players (Sorted A-Z)">
                {fromTeamPlayers.map(p => (
                  <option key={p.identity} value={p.identity}>{p.last}, {p.first} ({p.position})</option>
                ))}
              </optgroup>
            </select>
            <select 
              multiple 
              className="border-2 border-white w-full h-24 text-xs rounded-lg p-2 bg-white focus:ring-2 focus:ring-blue-200 outline-none" 
              value={fromDraftPicks} 
              onChange={e => setFromDraftPicks(Array.from(e.target.selectedOptions, o => o.value))}
            >
              <optgroup label="Draft Picks (By Year)">
                {fromTeamDraftPicks.map(p => (
                  <option key={p.overall} value={p.overall}>{p.year} Rd {p.round} (#{p.overall})</option>
                ))}
              </optgroup>
            </select>
          </div>
        </div>

        {/* RECEIVER ASSETS */}
        <div className="p-4 bg-red-50/50 rounded-lg border border-red-100">
          <p className="text-[11px] font-black text-red-700 uppercase mb-3 text-center">Partner Assets ({partnerCode || '...' })</p>
          <div className="space-y-3">
            <select 
              multiple 
              className="border-2 border-white w-full h-32 text-xs rounded-lg p-2 bg-white focus:ring-2 focus:ring-red-200 outline-none" 
              value={toPlayers} 
              onChange={e => setToPlayers(Array.from(e.target.selectedOptions, o => o.value))}
            >
              <optgroup label="Players (Sorted A-Z)">
                {toTeamPlayers.map(p => (
                  <option key={p.identity} value={p.identity}>{p.last}, {p.first} ({p.position})</option>
                ))}
              </optgroup>
            </select>
            <select 
              multiple 
              className="border-2 border-white w-full h-24 text-xs rounded-lg p-2 bg-white focus:ring-2 focus:ring-red-200 outline-none" 
              value={toDraftPicks} 
              onChange={e => setToDraftPicks(Array.from(e.target.selectedOptions, o => o.value))}
            >
              <optgroup label="Draft Picks (By Year)">
                {toTeamDraftPicks.map(p => (
                  <option key={p.overall} value={p.overall}>{p.year} Rd {p.round} (#{p.overall})</option>
                ))}
              </optgroup>
            </select>
          </div>
        </div>
      </div>

      <button 
        onClick={handleTrade} 
        disabled={loading || !toTeam} 
        className="w-full bg-purple-600 text-white p-4 rounded-xl font-black hover:bg-purple-700 disabled:bg-gray-200 transition-all uppercase tracking-widest shadow-lg flex justify-center items-center gap-2"
      >
        {loading ? 'Processing...' : 'Submit Official Trade'}
      </button>

      {status && (
        <div className={`p-4 rounded-lg text-xs font-bold text-center border animate-in fade-in slide-in-from-bottom-2 ${
          status.includes('✅') 
            ? 'bg-green-50 border-green-200 text-green-700 shadow-sm' 
            : 'bg-red-50 border-red-200 text-red-600 shadow-sm'
        }`}>
          {status}
        </div>
      )}
    </div>
  );
}
</file>

<file path="components/Navbar.tsx">
'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';

export default function Navbar() {
  const pathname = usePathname();

  const navItems = [
    { name: 'Front Office', href: '/' },
    { name: 'Rosters', href: '/rosters' },
    { name: 'Transactions', href: '/transactions' }, 
    { name: 'Draft Board', href: '/draft' },
    { name: 'Cuts', href: '/cuts' },
    // NEW: Added Constitution to the main array
    { name: 'Constitution', href: '/rules' }, 
  ];

  return (
    <nav className="w-full bg-slate-900 text-white shadow-md sticky top-0 z-[100] border-b border-slate-800">
      <div className="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">
        <div className="flex items-center gap-10">
          <Link href="/" className="text-xl font-bold tracking-tighter hover:opacity-90 transition-opacity">
            GFL<span className="text-blue-400">MANAGER</span>
          </Link>
          
          <div className="flex space-x-6">
            {navItems.map((item) => {
              const isActive = pathname === item.href;
              const isRules = item.href === '/rules';

              return (
                <Link
                  key={item.name}
                  href={item.href}
                  className={`text-sm font-medium transition-all hover:text-blue-400 ${
                    isActive 
                      ? 'text-blue-400' 
                      : isRules 
                        ? 'text-slate-400 italic' // Subtly distinguish the rules link
                        : 'text-slate-300'
                  }`}
                >
                  {item.name}
                </Link>
              );
            })}
          </div>
        </div>

        {/* Optional: Add a small badge or year indicator to the right */}
        <div className="hidden md:block">
          <span className="text-[10px] font-black bg-blue-500/10 text-blue-400 border border-blue-500/20 px-2 py-1 rounded tracking-widest uppercase">
            Season 2026
          </span>
        </div>
      </div>
    </nav>
  );
}
</file>

<file path="app/api/transactions/route.ts">
import { sheets, SHEET_ID } from '@/lib/googleSheets';
import { parsePlayers } from '@/lib/players';
import { findPlayerRowIndex } from '@/lib/playerLookup';
import { logTransaction } from '@/lib/transactions';

export const dynamic = 'force-dynamic';

export async function GET() {
  try {
    const response = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Transactions!A:J', 
    });

    const rows = response.data.values || [];
    if (rows.length === 0) return Response.json([]);

    // Mapping based on your Google Sheet columns
    const data = rows.slice(1).map((row) => ({
      timestamp: row[0] || '',
      type: row[1] || '',
      details: row[2] || '',      // Column C: "Desc" (The Description)
      fromFull: row[3] || '',     // Column D
      toFull: row[4] || '',       // Column E
      fromShort: row[5] || '',    // Column F
      toShort: row[6] || '',      // Column G
      coach: row[7] || '',        // Column H: "Owner" (Coach Name)
      status: row[8] || '',       // Column I
      weekBack: row[9] || ''      // Column J
    })).reverse();

    return Response.json(data);
  } catch (error: any) {
    return Response.json({ error: error.message }, { status: 500 });
  }
}

export async function POST(req: Request) {
  try {
    const body = await req.json();
    const { type, identity, toTeam, coach, details, status, weekBack, fromTeam } = body;

    const res = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID, range: 'Players',
    });

    const rows = res.data.values || [];
    const player = parsePlayers(rows).find(p => p.identity.trim().toLowerCase() === identity.trim().toLowerCase());

    if (!player) return Response.json({ error: 'Player not found' }, { status: 404 });

    const rowIndex = findPlayerRowIndex(rows, player);
    let newTeamValue = player.team;

    if (type === 'ADD' || type === 'INJURY PICKUP') newTeamValue = toTeam;
    else if (type === 'DROP') newTeamValue = 'FA';
    else if (type === 'IR') newTeamValue = `${player.team}-IR`;

    await sheets.spreadsheets.values.update({
      spreadsheetId: SHEET_ID,
      range: `Players!A${rowIndex}`,
      valueInputOption: 'RAW',
      requestBody: { values: [[newTeamValue]] },
    });

    await logTransaction({
      ...body,
      fromTeam: fromTeam || player.team,
      details: details 
    });

    return Response.json({ success: true });
  } catch (err: any) {
    return Response.json({ error: err.message }, { status: 500 });
  }
}
</file>

<file path="app/rosters/page.tsx">
'use client';

import React, { useState, useEffect, useMemo } from 'react';
import PlayerCard from '@/components/PlayerCard';

export const dynamic = 'force-dynamic';

const positionWeights: Record<string, number> = {
  'QB': 1, 'RB': 2, 'HB': 2, 'FB': 3, 'WR': 4, 'TE': 5,
  'OT': 6, 'LT': 6, 'RT': 6, 'T': 6, 'OG': 7, 'LG': 7, 'RG': 7, 'G': 7, 'C': 8, 'OL': 9,
  'DT': 10, 'NT': 11, 'DE': 12, 'DL': 13, 'MLB': 14, 'ILB': 15, 'OLB': 16, 'LB': 17,
  'CB': 18, 'FS': 19, 'SS': 20, 'S': 21, 'DB': 22,
  'K': 30, 'P': 31, 'LS': 32, 'KR': 33, 'PR': 34
};

export default function RosterPage() {
  const [teams, setTeams] = useState<any[]>([]);
  const [selectedTeam, setSelectedTeam] = useState('');
  const [data, setData] = useState<{ roster: any[], picks: any[] } | null>(null);
  const [loading, setLoading] = useState(false);
  const [sortBy, setSortBy] = useState<'default' | 'name' | 'pos'>('default');
  const [viewingPlayer, setViewingPlayer] = useState<any>(null);

  useEffect(() => {
    fetch('/api/teams').then(res => res.json()).then(setTeams);
  }, []);

  useEffect(() => {
    if (!selectedTeam) { setData(null); return; }
    setLoading(true);
    fetch(`/api/rosters/${selectedTeam}`)
      .then(res => res.json())
      .then(json => {
        setData({ roster: json.roster || [], picks: json.picks || [] });
        setLoading(false);
      });
  }, [selectedTeam]);

  const getGoogleSearchUrl = (name: string) => `https://www.google.com/search?q=${encodeURIComponent(name + ' NFL')}`;

  const fetchPlayerDetails = async (p: any) => {
    try {
      const nameParts = p.name ? p.name.trim().split(/\s+/) : ['', ''];
      const first = (nameParts[0] || '').toLowerCase();
      const last = (nameParts.length > 1 ? nameParts[nameParts.length - 1] : '').toLowerCase();
      const age = p.age ? String(p.age).toLowerCase() : '';
      const off = p.group === 'OFF' ? p.pos.toLowerCase() : '';
      const def = p.group === 'DEF' ? p.pos.toLowerCase() : '';
      const spec = p.group === 'SPEC' ? p.pos.toLowerCase() : '';

      const identity = [first, last, age, off, def, spec].join('|');
      const r = await fetch(`/api/players/details/${encodeURIComponent(identity)}`);
      if (r.ok) {
        setViewingPlayer(await r.json());
      } else {
        alert(`Scouting data not found for: ${p.name}`);
      }
    } catch (e) { console.error("Search Error:", e); }
  };

  const sortedGroups = useMemo(() => {
    if (!data?.roster) return { OFF: [], DEF: [], SPEC: [] };
    const sortFn = (players: any[]) => {
      const list = [...players];
      if (sortBy === 'name') return list.sort((a, b) => a.name.split(' ').pop().localeCompare(b.name.split(' ').pop()));
      if (sortBy === 'pos') return list.sort((a, b) => a.pos.localeCompare(b.pos));
      return list.sort((a, b) => (positionWeights[a.pos] || 99) - (positionWeights[b.pos] || 99));
    };
    return {
      OFF: sortFn(data.roster.filter(p => p.group === 'OFF')),
      DEF: sortFn(data.roster.filter(p => p.group === 'DEF')),
      SPEC: sortFn(data.roster.filter(p => p.group === 'SPEC')),
    };
  }, [data, sortBy]);

  return (
    <div className="max-w-7xl mx-auto p-4 md:p-8 space-y-6 bg-gray-50 min-h-screen text-slate-900">
      {viewingPlayer && <PlayerCard data={viewingPlayer} onClose={() => setViewingPlayer(null)} />}

      <div className="flex flex-col md:flex-row md:items-center justify-between gap-4 bg-white p-6 rounded-xl shadow-sm border border-gray-100">
        <div>
          <h1 className="text-2xl font-black tracking-tight text-slate-800">ROSTER EXPLORER</h1>
          <p className="text-xs font-bold text-blue-500 uppercase tracking-widest">Team Management & Scouting</p>
        </div>
        
        <div className="flex flex-wrap items-center gap-4">
          <div className="flex items-center bg-gray-100 p-1 rounded-lg border">
             <span className="text-[10px] font-black text-gray-400 uppercase px-2">Sort By</span>
             {(['default', 'name', 'pos'] as const).map((mode) => (
               <button key={mode} onClick={() => setSortBy(mode)}
                className={`px-3 py-1 text-xs font-bold rounded-md transition-all ${sortBy === mode ? 'bg-white text-blue-600 shadow-sm' : 'text-gray-500 hover:text-gray-700'}`}>
                 {mode.toUpperCase()}
               </button>
             ))}
          </div>

          <select value={selectedTeam} onChange={(e) => setSelectedTeam(e.target.value)}
            className="p-3 border rounded-lg bg-gray-50 font-bold text-gray-700 outline-none focus:ring-2 focus:ring-blue-500">
            <option value="">SELECT TEAM</option>
            {teams.map((t: any) => <option key={t.short} value={t.short}>{t.name}</option>)}
          </select>
        </div>
      </div>

      {loading ? (
        <div className="flex justify-center py-20 text-blue-600">
          <div className="w-10 h-10 border-4 border-current border-t-transparent rounded-full animate-spin"></div>
        </div>
      ) : data && (
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
          {(['OFF', 'DEF'] as const).map((unit) => (
            <div key={unit} className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden h-fit">
              <div className={`px-4 py-3 font-black text-white flex justify-between items-center ${unit === 'OFF' ? 'bg-slate-800' : 'bg-red-900'}`}>
                <span className="tracking-tighter">{unit === 'OFF' ? 'OFFENSE' : 'DEFENSE'}</span>
                <span className="bg-white/10 px-2 py-0.5 rounded text-[10px]">{sortedGroups[unit].length} PLYRS</span>
              </div>
              <div className="divide-y divide-gray-100">
                {sortedGroups[unit].map((p, i) => (
                  <div key={i} className="flex items-center justify-between p-3 hover:bg-blue-50/50 group transition-colors">
                    <div className="flex items-center gap-3">
                      <span className="font-mono text-[11px] font-black text-blue-600 bg-blue-50 px-2 py-1 rounded w-10 text-center">{p.pos}</span>
                      <div className="flex flex-col">
                        <a href={getGoogleSearchUrl(p.name)} target="_blank" rel="noopener noreferrer" 
                           className="text-sm font-bold text-slate-800 uppercase hover:text-blue-600 hover:underline decoration-2 underline-offset-4">
                          {p.name}
                        </a>
                        <span className="text-[10px] font-bold text-gray-400">AGE: {p.age || '—'}</span>
                      </div>
                    </div>
                    <button onClick={() => fetchPlayerDetails(p)}
                      className="opacity-0 group-hover:opacity-100 bg-white text-blue-700 text-[10px] font-black px-3 py-1.5 rounded-lg border shadow-sm hover:bg-blue-600 hover:text-white transition-all">
                      Details
                    </button>
                  </div>
                ))}
              </div>
            </div>
          ))}

          <div className="space-y-6">
            {/* Special Teams Card */}
            <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
              <div className="px-4 py-3 font-black text-white bg-indigo-900 flex justify-between items-center">
                <span className="tracking-tighter uppercase">Special Teams</span>
                <span className="bg-white/10 px-2 py-0.5 rounded text-[10px]">{sortedGroups.SPEC.length}</span>
              </div>
              <div className="divide-y divide-gray-100">
                {sortedGroups.SPEC.map((p, i) => (
                  <div key={i} className="flex items-center justify-between p-3 hover:bg-indigo-50/50 group transition-colors">
                    <div className="flex items-center gap-3">
                      <span className="font-mono text-[11px] font-black text-indigo-600 bg-indigo-50 px-2 py-1 rounded w-10 text-center">{p.pos}</span>
                      <a href={getGoogleSearchUrl(p.name)} target="_blank" rel="noopener noreferrer" 
                         className="text-sm font-bold text-slate-800 uppercase hover:text-indigo-600 hover:underline">{p.name}</a>
                    </div>
                    <button onClick={() => fetchPlayerDetails(p)}
                      className="opacity-0 group-hover:opacity-100 bg-white text-indigo-700 text-[10px] font-black px-3 py-1.5 rounded-lg border shadow-sm hover:bg-indigo-600 hover:text-white transition-all">
                      SCOUT
                    </button>
                  </div>
                ))}
              </div>
            </div>

            {/* Redesigned Draft Capital Card */}
            <div className="bg-slate-900 rounded-2xl shadow-xl p-5 border border-slate-800">
               <div className="flex justify-between items-center mb-4">
                  <h3 className="font-black text-white text-xs tracking-[0.2em] uppercase">Draft Capital</h3>
                  <span className="text-slate-500 font-mono text-xs">{data.picks?.length || 0} ASSETS</span>
               </div>
               <div className="space-y-3">
                 {data.picks?.map((pick: any, i: number) => (
                   <div key={i} className="relative bg-slate-800/50 border border-slate-700 p-3 rounded-xl flex items-center gap-4 overflow-hidden group">
                     <div className="absolute top-0 right-0 p-2 opacity-10 font-black text-4xl italic text-white pointer-events-none">
                       {pick.round}
                     </div>
                     <div className="bg-blue-600 text-white w-12 h-12 rounded-lg flex flex-col items-center justify-center flex-shrink-0 shadow-lg">
                        <span className="text-[10px] font-black leading-none">{pick.year}</span>
                        <span className="text-lg font-black italic">R{pick.round}</span>
                     </div>
                     <div className="flex-grow">
                       <p className="text-white font-black text-sm tracking-tight">
                         {pick.overall && pick.overall !== 'N/A' ? `PICK #${pick.overall}` : 'ROUND ' + pick.round}
                       </p>
                       <p className="text-[10px] font-bold text-slate-400 uppercase tracking-tighter">
                         From: <span className="text-blue-400">{pick.originalTeam}</span>
                       </p>
                     </div>
                   </div>
                 ))}
               </div>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
</file>

<file path="app/api/cuts/route.ts">
import { NextRequest, NextResponse } from 'next/server';
import { sheets, SHEET_ID } from '@/lib/googleSheets';

export const dynamic = 'force-dynamic';
export const revalidate = 0;

export async function GET(req: NextRequest) {
  const { searchParams } = new URL(req.url);
  const team = searchParams.get('team');
  const yearParam = searchParams.get('year');

  try {
    const result = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Cuts!A:J',
    });

    const rows = result.data.values || [];
    const leagueSummary: Record<string, { protected: number, pullback: number, lastUpdated: string }> = {};
    const selections: Record<string, string> = {};
    let lastTime = "";

    rows.slice(1).forEach((row) => {
      if (!row || row.length < 2) return;

      const rowYear = String(row[0] || '').trim();
      const rowTeam = String(row[1] || '').trim();
      const targetYear = String(yearParam || '').trim();

      if (rowYear === targetYear) {
        // 1. LEAGUE SUMMARY
        if (rowTeam) {
          if (!leagueSummary[rowTeam]) {
            leagueSummary[rowTeam] = { protected: 0, pullback: 0, lastUpdated: "" };
          }
          const status = String(row[8] || '').trim().toLowerCase();
          if (status === 'protected') leagueSummary[rowTeam].protected++;
          if (status === 'pullback') leagueSummary[rowTeam].pullback++;
          
          if (row[9] && (!leagueSummary[rowTeam].lastUpdated || row[9] > leagueSummary[rowTeam].lastUpdated)) {
            leagueSummary[rowTeam].lastUpdated = row[9];
          }
        }

        // 2. PLAYER SELECTIONS (Identity Mapping)
        if (team && rowTeam.toLowerCase() === String(team).trim().toLowerCase()) {
          // INDEX MAP BASED ON YOUR SHEET:
          // 2:First, 3:Last, 4:Age, 5:Off, 6:Def, 7:Spec
          const identity = [2, 3, 4, 5, 6, 7]
            .map(i => String(row[i] || '').trim().toLowerCase())
            .join('|');
          
          selections[identity] = String(row[8] || '').trim().toLowerCase();
          
          if (row[9] && (!lastTime || row[9] > lastTime)) {
            lastTime = row[9];
          }
        }
      }
    });

    return NextResponse.json({ 
      summary: leagueSummary, 
      selections, 
      lastUpdated: lastTime 
    }, {
      headers: { 'Cache-Control': 'no-store, max-age=0, must-revalidate' }
    });

  } catch (err: any) {
    console.error('[DEBUG] GET ERROR:', err.message);
    return NextResponse.json({ error: err.message, summary: {}, selections: {} }, { status: 500 });
  }
}

export async function POST(req: NextRequest) {
  try {
    const { team, year, selections } = await req.json();
    
    const timestamp = new Date().toLocaleString('en-US', { 
      timeZone: 'America/New_York',
      hour12: false 
    }).replace(',', '');

    const currentSheet = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Cuts!A:J', 
    });

    const allRows = currentSheet.data.values || [];
    // Updated Header to match your actual sheet columns
    const header = allRows[0] || ["Year", "Team", "First", "Last", "Age", "Offense", "Defense", "Special", "Status", "Timestamp"]; 

    const otherRows = allRows.slice(1).filter((row) => {
      const isMatch = String(row[0]).trim() === String(year).trim() && 
                      String(row[1]).trim() === String(team).trim();
      return !isMatch;
    });

    const newTeamRows = selections.map((p: any) => {
      const parts = p.identity.split('|'); // Splits back into [First, Last, Age, Off, Def, Spec]
      const statusFormatted = p.status.charAt(0).toUpperCase() + p.status.slice(1);
      
      // Structure: Year, Team, First, Last, Age, Offense, Defense, Special, Status, Timestamp
      return [year, team, ...parts, statusFormatted, timestamp];
    });

    await sheets.spreadsheets.values.update({
      spreadsheetId: SHEET_ID,
      range: 'Cuts!A1',
      valueInputOption: 'USER_ENTERED',
      requestBody: { 
        values: [header, ...otherRows, ...newTeamRows] 
      },
    });

    return NextResponse.json({ success: true });
  } catch (err: any) {
    console.error('[DEBUG] POST ERROR:', err.message);
    return NextResponse.json({ error: err.message }, { status: 500 });
  }
}
</file>

<file path="app/draft/page.tsx">
'use client';

import React, { useState, useEffect, useCallback, useMemo } from 'react';
import SelectionModal from '@/components/SelectionModal';
import PlayerCard from '@/components/PlayerCard'; 

export const dynamic = 'force-dynamic';

interface Team { name: string; short: string; }

export default function DraftPage() {
  const [picks, setPicks] = useState<any[]>([]);
  const [teams, setTeams] = useState<Team[]>([]);
  const [loading, setLoading] = useState(true);
  const [isRefreshing, setIsRefreshing] = useState(false);
  
  // Filter States
  const [searchTerm, setSearchTerm] = useState('');
  const [yearFilter, setYearFilter] = useState('All');
  const [teamFilter, setTeamFilter] = useState('All Teams');
  const [roundFilter, setRoundFilter] = useState('All Rounds');
  
  // Free Agent States
  const [showFA, setShowFA] = useState(false);
  const [faPlayers, setFaPlayers] = useState<any[]>([]);
  const [faLoading, setFaLoading] = useState(false);
  const [faSearch, setFaSearch] = useState('');
  
  // Selection & Scouting States
  const [selectedPlayer, setSelectedPlayer] = useState<any>(null); 
  const [selectedPick, setSelectedPick] = useState<any>(null);
  const [showSelectionModal, setShowSelectionModal] = useState(false);

  // Timer States
  const [timeLeft, setTimeLeft] = useState<string>("00:00:00");
  const [progress, setProgress] = useState<number>(100);

  const loadData = useCallback(async (showRefreshState = false) => {
    if (showRefreshState) setIsRefreshing(true);
    try {
      const [pRes, tRes] = await Promise.all([
        fetch('/api/draft-picks', { cache: 'no-store' }).then(res => res.json()),
        fetch('/api/teams').then(res => res.json())
      ]);
      const sortedPicks = Array.isArray(pRes) 
        ? pRes.sort((a, b) => Number(a.overall) - Number(b.overall))
        : [];
      setPicks(sortedPicks);
      setTeams(tRes);
    } catch (err) { 
      console.error("Error:", err); 
    } finally { 
      setLoading(false); 
      setIsRefreshing(false);
    }
  }, []);

  useEffect(() => { loadData(); }, [loadData]);

  const { onClockPick, previousPick } = useMemo(() => {
    const currentIndex = picks.findIndex(p => p.status === "Active");
    return {
      onClockPick: picks[currentIndex],
      previousPick: currentIndex > 0 ? picks[currentIndex - 1] : null
    };
  }, [picks]);

  useEffect(() => {
    if (!onClockPick) return;
    const timerInterval = setInterval(() => {
      const now = new Date().getTime();
      const startTimeStr = previousPick?.timestamp;
      const startRef = startTimeStr ? new Date(startTimeStr).getTime() : new Date().getTime() - (1000 * 60); 
      const roundNum = parseInt(onClockPick.round);
      const limitMs = (roundNum <= 2 ? 24 : 12) * 60 * 60 * 1000;
      const expiryTime = startRef + limitMs;
      const diff = expiryTime - now;

      if (diff <= 0) {
        setTimeLeft("EXPIRED");
        setProgress(0);
      } else {
        const h = Math.floor(diff / (1000 * 60 * 60));
        const m = Math.floor((diff / (1000 * 60)) % 60);
        const s = Math.floor((diff / 1000) % 60);
        setTimeLeft(`${h.toString().padStart(2, '0')}:${m.toString().padStart(2, '0')}:${s.toString().padStart(2, '0')}`);
        setProgress((diff / limitMs) * 100);
      }
    }, 1000);
    return () => clearInterval(timerInterval);
  }, [onClockPick, previousPick]);

  const resolveCode = (str: string) => {
    if (!str) return "";
    const match = str.match(/\(([^)]+)\)/);
    return (match ? match[1] : str).trim().toUpperCase();
  };

  const getFullTeamName = (shortCode: string) => {
    if (!shortCode) return "Unknown Team";
    const codeToMatch = resolveCode(shortCode);
    const team = teams.find(t => resolveCode(t.short) === codeToMatch);
    return team ? team.name : shortCode;
  };

  const filteredPicks = useMemo(() => {
    return picks.filter(p => {
      const searchStr = searchTerm.toLowerCase();
      const matchesSearch = p.currentOwner?.toLowerCase().includes(searchStr) || 
                            p.draftedPlayer?.toLowerCase().includes(searchStr);
      const matchesYear = yearFilter === 'All' || p.year?.toString() === yearFilter;
      const matchesTeam = teamFilter === 'All Teams' || getFullTeamName(p.currentOwner) === teamFilter;
      const matchesRound = roundFilter === 'All Rounds' || p.round.toString() === roundFilter;
      return matchesSearch && matchesYear && matchesTeam && matchesRound;
    });
  }, [picks, searchTerm, yearFilter, teamFilter, roundFilter, teams]);

  const fetchFAWithDetails = async (p: any) => {
    try {
      const first = (p.first || '').toLowerCase();
      const last = (p.last || '').toLowerCase();
      const age = p.age ? String(p.age).toLowerCase() : '';
      const off = (p.offense || '').toLowerCase();
      const def = (p.defense || '').toLowerCase();
      const spec = (p.special || '').toLowerCase();
      const identity = [first, last, age, off, def, spec].join('|');
      
      const r = await fetch(`/api/players/details/${encodeURIComponent(identity)}`);
      if (r.ok) {
        const detailData = await r.json();
        setSelectedPlayer(detailData);
      } else {
        alert(`Scouting data not found for: ${p.first} ${p.last}`);
      }
    } catch (e) {
      console.error("SEARCH ERROR:", e);
    }
  };

  // Helper to open the modal
  const handleOpenSelection = (pick: any) => {
    setSelectedPick(pick);
    setShowSelectionModal(true);
  };

  if (loading) return <div className="p-20 text-center font-black text-slate-400 uppercase tracking-widest">Loading Draft Board...</div>;

  return (
    <div className="max-w-7xl mx-auto p-4 md:p-8 space-y-6 bg-gray-50 min-h-screen font-sans text-black">
      
      {/* CLOCK SECTION */}
      {onClockPick && (
        <div className="bg-slate-900 rounded-2xl shadow-2xl overflow-hidden border border-slate-800">
          <div className="p-6 md:p-10 flex flex-col md:flex-row justify-between items-center gap-8">
            <div className="space-y-2 text-center md:text-left">
              <span className="bg-blue-600 text-white text-[10px] font-black px-3 py-1 rounded-full uppercase tracking-tighter animate-pulse">Live: On the Clock</span>
              <h2 className="text-4xl font-black text-white uppercase italic tracking-tighter leading-tight">
                {getFullTeamName(onClockPick.currentOwner)}
                {(onClockPick.originalTeam || onClockPick.original_team) && (onClockPick.originalTeam || onClockPick.original_team) !== onClockPick.currentOwner && (
                  <span className="block text-sm text-blue-400 font-bold normal-case italic mt-1">
                    via {getFullTeamName(onClockPick.originalTeam || onClockPick.original_team)}
                  </span>
                )}
              </h2>
              <p className="text-slate-400 font-bold text-sm">Overall Pick #{onClockPick.overall} • Round {onClockPick.round}</p>
            </div>
            <div className="flex flex-col items-center md:items-end">
              <p className="text-5xl md:text-7xl font-mono font-black text-amber-400 tracking-tighter tabular-nums">{timeLeft}</p>
              <p className="text-[10px] font-black text-slate-500 uppercase mt-2 tracking-widest">Remaining for this pick</p>
            </div>
          </div>
          <div className="h-2 bg-slate-800 w-full">
            <div className={`h-full transition-all duration-1000 ease-linear ${progress < 20 ? 'bg-red-500' : 'bg-blue-500'}`} style={{ width: `${progress}%` }} />
          </div>
        </div>
      )}

      {/* FILTER BAR */}
      <div className="flex flex-col xl:flex-row gap-4">
        <div className="grid grid-cols-1 md:grid-cols-4 gap-4 bg-white p-4 rounded-xl shadow-sm border border-gray-100 flex-1">
          <div className="flex flex-col gap-1">
            <label className="text-[10px] font-black uppercase text-slate-400 ml-1">Year</label>
            <select className="p-3 border rounded-lg bg-gray-50 text-xs font-bold uppercase text-black" value={yearFilter} onChange={(e) => setYearFilter(e.target.value)}>
              <option value="All">All Years</option>
              {Array.from(new Set(picks.map(p => p.year))).sort().map(y => <option key={y} value={y}>{y}</option>)}
            </select>
          </div>
          <div className="flex flex-col gap-1">
            <label className="text-[10px] font-black uppercase text-slate-400 ml-1">Team Owner</label>
            <select className="p-3 border rounded-lg bg-gray-50 text-xs font-bold uppercase text-black" value={teamFilter} onChange={(e) => setTeamFilter(e.target.value)}>
              <option value="All Teams">All Teams</option>
              {Array.from(new Set(picks.map(p => getFullTeamName(p.currentOwner)))).sort().map(t => <option key={t} value={t}>{t}</option>)}
            </select>
          </div>
          <div className="flex flex-col gap-1">
            <label className="text-[10px] font-black uppercase text-slate-400 ml-1">Round</label>
            <select className="p-3 border rounded-lg bg-gray-50 text-xs font-bold uppercase text-black" value={roundFilter} onChange={(e) => setRoundFilter(e.target.value)}>
              <option value="All Rounds">All Rounds</option>
              {Array.from(new Set(picks.map(p => p.round))).sort((a,b)=>a-b).map(r => <option key={r} value={r.toString()}>Round {r}</option>)}
            </select>
          </div>
          <div className="flex flex-col gap-1">
            <label className="text-[10px] font-black uppercase text-slate-400 ml-1">Search</label>
            <input type="text" placeholder="Search player..." className="p-3 border rounded-lg text-sm text-black" value={searchTerm} onChange={(e) => setSearchTerm(e.target.value)} />
          </div>
        </div>
        
        <div className="flex gap-2">
            <button onClick={() => { setFaLoading(true); setShowFA(true); fetch('/api/free-agents').then(r => r.json()).then(res => { setFaPlayers(res); setFaLoading(false); }); }} className="flex-1 xl:flex-none bg-slate-900 text-white px-6 py-4 rounded-xl shadow-sm font-black uppercase text-xs tracking-widest hover:bg-slate-800 active:scale-95">
                Free Agents
            </button>
            <button onClick={() => loadData(true)} disabled={isRefreshing} className="flex-1 xl:flex-none bg-white border border-gray-200 hover:bg-gray-50 text-slate-600 px-6 py-4 rounded-xl shadow-sm flex items-center justify-center gap-3 transition-all active:scale-95 disabled:opacity-50">
                <svg className={`w-4 h-4 ${isRefreshing ? 'animate-spin' : ''}`} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="3" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
                </svg>
                <span className="text-xs font-black uppercase tracking-widest text-black">Refresh Board</span>
            </button>
        </div>
      </div>

      {/* DRAFT TABLE */}
      <div className="bg-white rounded-2xl shadow-xl border border-gray-200 overflow-hidden">
        <div className="overflow-x-auto">
          <table className="w-full text-left border-collapse">
            <thead>
              <tr className="bg-slate-900 text-white uppercase text-[10px] tracking-widest">
                <th className="px-6 py-4">Pick</th>
                <th className="px-6 py-4">Drafted Player</th>
                <th className="px-6 py-4">Team Owner</th>
                <th className="px-6 py-4 text-center">Status</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-gray-100">
              {filteredPicks.map((pick) => {
                const isDrafted = !!pick.draftedPlayer && !pick.draftedPlayer.includes("SKIPPED");
                const isSkipped = !!pick.draftedPlayer && pick.draftedPlayer.includes("SKIPPED");
                const isOnClock = onClockPick && pick.overall === onClockPick.overall;
                const originalTeam = pick.originalTeam || pick.original_team;
                const isTraded = originalTeam && originalTeam !== pick.currentOwner;
                
                return (
                  <tr key={pick.overall} className={`transition-colors ${isOnClock ? 'bg-blue-50/80 ring-2 ring-blue-500 ring-inset' : ''}`}>
                    <td className="px-6 py-6">
                      <span className={`text-2xl font-black ${isOnClock ? 'text-blue-600' : 'text-slate-200'}`}>#{pick.overall}</span>
                    </td>
                    <td className="px-6 py-4 text-black">
                      {isDrafted ? (
                        <div className="flex flex-col">
                          <a 
                            href={`https://www.google.com/search?q=${encodeURIComponent(pick.draftedPlayer )}`} 
                            target="_blank" 
                            rel="noopener noreferrer"
                            className="font-black text-slate-900 uppercase text-sm hover:text-blue-600 hover:underline transition-colors"
                          >
                            {pick.draftedPlayer}
                          </a>
                          <span className="text-[9px] font-bold text-slate-400 uppercase">{pick.timestamp}</span>
                        </div>
                      ) : isSkipped ? (
                        <div className="flex flex-col text-orange-500 uppercase">
                          <span className="font-black text-[10px]">Time Expired (Skipped)</span>
                          <span className="text-[9px] font-bold text-slate-400 uppercase italic">Can still make late selection</span>
                        </div>
                      ) : (
                        <span className="text-slate-300 italic text-xs uppercase font-bold tracking-widest">{isOnClock ? 'On the Clock' : 'Pending'}</span>
                      )}
                    </td>
                    <td className="px-6 py-4">
                      <div className="flex flex-col">
                        <span className={`font-black uppercase ${isOnClock ? 'text-blue-900' : 'text-slate-700'}`}>{getFullTeamName(pick.currentOwner)}</span>
                        {isTraded && (
                          <span className="text-[10px] text-blue-600 font-bold italic mt-0.5">via {getFullTeamName(originalTeam)}</span>
                        )}
                      </div>
                    </td>
                    <td className="px-6 py-4 text-center">
                      {(isOnClock || isSkipped) && !isDrafted ? (
                        <button onClick={() => handleOpenSelection(pick)} className={`${isOnClock ? "bg-blue-600 hover:bg-blue-700 animate-pulse shadow-[0_0_15px_rgba(37,99,235,0.3)]" : "bg-[#f59e0b] hover:bg-[#d97706]"} text-white text-[10px] font-black py-2.5 px-5 rounded-lg shadow-lg uppercase transition-all`}>
                          {isSkipped ? "Make Late Selection" : "Make Selection"}
                        </button>
                      ) : isDrafted ? (
                        <span className="text-green-500 font-black text-[10px] border border-green-200 bg-green-50 px-3 py-1 rounded-full uppercase">Completed</span>
                      ) : (
                        <span className="text-slate-300 font-black text-[10px] uppercase">Locked</span>
                      )}
                    </td>
                  </tr>
                );
              })}
            </tbody>
          </table>
        </div>
      </div>

      {/* FREE AGENT DRAWER */}
      {showFA && (
        <div className="fixed inset-0 z-[80] flex justify-end">
          <div className="absolute inset-0 bg-slate-900/60 backdrop-blur-sm" onClick={() => setShowFA(false)} />
          <div className="relative w-full max-w-md bg-white h-full shadow-2xl flex flex-col border-l border-slate-200">
            <div className="p-6 bg-slate-900 text-white flex justify-between items-center">
              <div>
                <h3 className="text-lg font-black uppercase tracking-tight">Free Agents</h3>
                <p className="text-[10px] text-slate-400 font-bold uppercase tracking-widest">Waiver Wire</p>
              </div>
              <button onClick={() => setShowFA(false)} className="text-3xl">&times;</button>
            </div>
            <div className="p-4 border-b">
              <input type="text" placeholder="Search FA..." className="w-full p-3 border rounded-xl text-black text-sm" value={faSearch} onChange={(e) => setFaSearch(e.target.value)} />
            </div>
            <div className="flex-1 overflow-y-auto p-4 space-y-3">
              {faLoading ? (
                <div className="text-center py-20 animate-pulse text-slate-400 font-black uppercase">Loading...</div>
              ) : faPlayers.filter(p => `${p.first} ${p.last}`.toLowerCase().includes(faSearch.toLowerCase())).map((p, i) => (
                <div key={i} className="p-4 border rounded-2xl hover:bg-slate-50 transition-colors flex justify-between items-center group text-black">
                  <div className="flex-1">
                    <a 
                      href={`https://www.google.com/search?q=${encodeURIComponent(p.first + ' ' + p.last )}`}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="font-black text-slate-900 uppercase text-sm hover:text-blue-600 hover:underline inline-flex items-center gap-1 group/link"
                    >
                      {p.first} {p.last}
                      <svg className="w-3 h-3 text-blue-400 opacity-0 group-hover/link:opacity-100 transition-opacity" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="3" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                      </svg>
                    </a>
                    <p className="text-[10px] text-slate-500 font-bold uppercase">Age {p.age} — {p.position}</p>
                  </div>
                  <button 
                    onClick={() => fetchFAWithDetails(p)} 
                    className="text-[10px] font-black uppercase text-blue-600 bg-blue-50 hover:bg-blue-100 px-3 py-2 rounded-xl shrink-0"
                  >
                    Details
                  </button>
                </div>
              ))}
            </div>
          </div>
        </div>
      )}

      {selectedPlayer && <PlayerCard data={selectedPlayer} onClose={() => setSelectedPlayer(null)} />}

      {showSelectionModal && selectedPick && (
        <SelectionModal 
          pick={{...selectedPick, currentOwner: getFullTeamName(selectedPick.currentOwner), currentOwnerCode: resolveCode(selectedPick.currentOwner)}}
          onClose={() => { setSelectedPick(null); setShowSelectionModal(false); }}
          onComplete={() => { setSelectedPick(null); setShowSelectionModal(false); loadData(); }}
          onScout={(p) => fetchFAWithDetails(p)} 
        />
      )}
    </div>
  );
}
</file>

<file path="app/api/trades/route.ts">
import { sheets, SHEET_ID } from '@/lib/googleSheets';
import { logTransaction } from '@/lib/transactions';

export async function POST(req: Request) {
  try {
    const body = await req.json();
    const { 
      fromTeam, toTeam, playersFrom, playersTo, 
      draftPicksFrom, draftPicksTo, submittedBy,
      rawIdentitiesFrom, rawIdentitiesTo 
    } = body;

    // 1. FETCH DATA
    const [playerRes, draftRes] = await Promise.all([
      sheets.spreadsheets.values.get({ spreadsheetId: SHEET_ID, range: 'Players!A:I' }),
      sheets.spreadsheets.values.get({ spreadsheetId: SHEET_ID, range: 'DraftPicks!A:G' })
    ]);

    const playerRows = playerRes.data.values || [];
    const draftRows = draftRes.data.values || [];

    // --- ASSET FORMATTER FOR LOGS ---
    // Added explicit string[] types here to prevent build errors
    const formatAssetString = (playerIdentities: string[], pickStrings: string[]) => {
      const titleCase = (str: string) => 
        str.toLowerCase().split(' ').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' ');

      const formattedPlayers = (playerIdentities || []).map(p => {
        if (p.includes('|')) {
          const parts = p.split('|');
          const firstName = titleCase(parts[0] || '');
          const lastName = titleCase(parts[1] || '');
          const position = parts.slice(2).find(part => 
            part && part.length >= 1 && part.length <= 5 && isNaN(Number(part))
          ) || 'PLAYER';
          return `${position.toUpperCase()} - ${firstName} ${lastName}`;
        }
        return titleCase(p);
      });

      return [...formattedPlayers, ...(pickStrings || [])].filter(Boolean).join(', ');
    };

    const cleanAssetsFrom = formatAssetString(rawIdentitiesFrom, draftPicksFrom);
    const cleanAssetsTo = formatAssetString(rawIdentitiesTo, draftPicksTo);

    // --- EXECUTE UPDATES ---
    const updatePromises: Promise<any>[] = [];

    const findPlayerRow = (id: string) => {
      if (!id || !id.includes('|')) return -1;
      const p = id.toLowerCase().split('|');

      return playerRows.findIndex(r => {
        const match = (sheetVal: any, identityVal: any) => {
          if (identityVal === undefined || identityVal === '') return true;
          return String(sheetVal || '').toLowerCase().trim() === String(identityVal).toLowerCase().trim();
        };

        return (
          match(r[2], p[0]) && // First Name (C)
          match(r[3], p[1]) && // Last Name (D)
          match(r[5], p[2]) && // Age (F)
          match(r[6], p[3]) && // Position 1 (G)
          match(r[7], p[4]) && // Position 2 (H)
          match(r[8], p[5])    // Position 3 (I)
        );
      });
    };

    const findDraftRow = (pickString: string) => {
      const match = pickString.match(/#(\d+)/);
      if (!match) return -1;
      return draftRows.findIndex(r => String(r[2]).trim() === String(match[1]).trim());
    };

    // 1. Update Proposer Assets -> Move to Partner Team
    rawIdentitiesFrom?.forEach((id: string) => {
      const idx = findPlayerRow(id);
      if (idx !== -1) updatePromises.push(updateCell('Players', 'A', idx + 1, toTeam));
      else console.warn(`Player not found for update: ${id}`);
    });
    
    // FIXED: Added string type to pick
    draftPicksFrom?.forEach((pick: string) => {
      const idx = findDraftRow(pick);
      if (idx !== -1) updatePromises.push(updateCell('DraftPicks', 'E', idx + 1, toTeam));
    });

    // 2. Update Partner Assets -> Move to Proposer Team
    rawIdentitiesTo?.forEach((id: string) => {
      const idx = findPlayerRow(id);
      if (idx !== -1) updatePromises.push(updateCell('Players', 'A', idx + 1, fromTeam));
      else console.warn(`Player not found for update: ${id}`);
    });

    // FIXED: Added string type to pick
    draftPicksTo?.forEach((pick: string) => {
      const idx = findDraftRow(pick);
      if (idx !== -1) updatePromises.push(updateCell('DraftPicks', 'E', idx + 1, fromTeam));
    });

    await Promise.all(updatePromises);

    // --- LOG TRANSACTIONS ---
    if (cleanAssetsFrom) {
      await logTransaction({
        type: 'TRADE',
        identity: cleanAssetsFrom, 
        fromTeam, toTeam, coach: submittedBy,
      });
    }

    if (cleanAssetsTo) {
      await logTransaction({
        type: 'TRADE',
        identity: cleanAssetsTo,
        fromTeam: toTeam,
        toTeam: fromTeam,
        coach: submittedBy,
      });
    }

    return Response.json({ success: true });

  } catch (error: any) {
    console.error("Trade API Error:", error);
    return Response.json({ success: false, error: error.message }, { status: 500 });
  }
}

async function updateCell(sheetName: string, column: string, rowNum: number, value: string) {
  return sheets.spreadsheets.values.update({
    spreadsheetId: SHEET_ID,
    range: `${sheetName}!${column}${rowNum}`,
    valueInputOption: 'RAW',
    requestBody: { values: [[value]] },
  });
}
</file>

<file path="lib/draftPicks.ts">
import { sheets, SHEET_ID } from './googleSheets';

const SHEET = 'DraftPicks';

// Updated to match your spreadsheet structure
export type DraftPick = {
  year: number;
  round: number;
  overall: number;     
  originalTeam: string; 
  currentOwner: string; 
  status: string;      
};

/**
 * Get all draft picks with full details
 */
export async function getAllDraftPicks(): Promise<DraftPick[]> {
  try {
    const res = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: `${SHEET}!A2:G`,
    });

    const rows = res.data.values;
    if (!Array.isArray(rows)) return [];

    return rows.map(r => ({
      year: Number(r[0]),
      round: Number(r[1]),
      overall: Number(r[2]),
      originalTeam: r[3] || '',
      currentOwner: r[4] || '',
      status: r[5] || 'Active',
    }));
  } catch (error) {
    console.error('getAllDraftPicks failed:', error);
    throw error;
  }
}

/**
 * Find a specific pick using Year, Round, and the Current Owner
 */
export async function findDraftPick(
  currentOwner: string,
  year: number,
  round: number,
  overall?: number // Added overall here for consistency
) {
  const rows = await getAllDraftPicks();

  return rows.find(
    r =>
      r.year === year &&
      r.round === round &&
      r.currentOwner.toLowerCase() === currentOwner.toLowerCase() &&
      (!overall || r.overall === overall) // Match pick number if provided
  );
}

/**
 * Transfer a draft pick by updating the 'Current Owner' (Column E)
 * Note: Index 4 is Column E. Index 5 is Column F. 
 * Based on your GET request in the route, currentOwner is index 4 (Col E).
 */
export async function transferDraftPick(
  fromTeam: string,
  toTeam: string,
  year: number,
  round: number,
  overall?: number
) {
  try {
    const res = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: `${SHEET}!A2:G`,
    });

    const rows = res.data.values || [];

    // Find the row. We prioritize 'overall' if it's available.
    const rowIndex = rows.findIndex(r => {
      const matchesYear = Number(r[0]) === year;
      const matchesRound = Number(r[1]) === round;
      const matchesOwner = r[4]?.toLowerCase() === fromTeam.toLowerCase();
      
      if (overall) {
        return matchesYear && matchesRound && Number(r[2]) === overall;
      }
      return matchesYear && matchesRound && matchesOwner;
    });

    if (rowIndex === -1) {
      throw new Error(`Pick ${year} R${round} (Pick #${overall}) not found for ${fromTeam}`);
    }

    const sheetRow = rowIndex + 2;

    // Update Column E (Index 4) which is the Current Owner
    await sheets.spreadsheets.values.update({
      spreadsheetId: SHEET_ID,
      range: `${SHEET}!E${sheetRow}`, 
      valueInputOption: 'RAW',
      requestBody: {
        values: [[toTeam]],
      },
    });
  } catch (error) {
    console.error('transferDraftPick failed:', error);
    throw error;
  }
}
</file>

<file path="lib/transactions.ts">
import { sheets, SHEET_ID } from './googleSheets';

export type Transaction = {
  type: string;
  identity: string;
  fromTeam?: string;
  toTeam?: string;
  coach: string;
  details?: string;
  status?: string;
  weekBack?: string;
};

/**
 * Updates the 'Current Owner' in the DraftPicks sheet using the Unique Overall Pick Number.
 * This is the most reliable method as Overall (#) is a unique identifier.
 */
// lib/transactions.ts - updatePickOwner function

/**
 * Updates the 'Current Owner' in the DraftPicks sheet.
 * Uses USER_ENTERED to ensure the UI refreshes and Google processes the change.
 */
async function updatePickOwner(details: string, newOwnerShort: string, fromTeamShort: string) {
  console.log("--- START Surgical Pick Update ---");
  
  try {
    // 1. Extract Overall Pick ID (e.g., "2" from "(#2)")
    const overallMatch = details.match(/\(#(\d+)\)/);
    if (!overallMatch) {
      console.error("❌ Surgical Match Failed: Identity missing (#ID).");
      return;
    }
    const targetOverall = overallMatch[1].trim();

    // 2. Fetch the current data
    const response = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'DraftPicks!A:G', 
    });
    const rows = response.data.values || [];

    // 3. Find the exact row based on Column C (Overall)
    const rowIndex = rows.findIndex(row => String(row[2] || '').trim() === targetOverall);

    if (rowIndex !== -1) {
      // Column E is index 4. This is the 'Current Owner' column.
      const rangeToUpdate = `DraftPicks!E${rowIndex + 1}`;
      const targetValue = newOwnerShort.toUpperCase().trim();

      console.log(`Writing "${targetValue}" to Range: ${rangeToUpdate}`);

      // 4. Perform the Update with USER_ENTERED
      const updateResponse = await sheets.spreadsheets.values.update({
        spreadsheetId: SHEET_ID,
        range: rangeToUpdate,
        valueInputOption: 'USER_ENTERED', // 💡 Forces the UI to refresh/re-calculate
        requestBody: {
          values: [[targetValue]],
        },
      });

      if (updateResponse.status === 200) {
        console.log(`✅ GOOGLE CONFIRMED: Cell at ${rangeToUpdate} updated.`);
        // Optional: Log the first few chars of the Sheet ID to verify you're in the right file
        console.log(`Targeting Sheet ID: ${SHEET_ID.substring(0, 5)}...`);
      }
    } else {
      console.error(`❌ NOT FOUND: Overall #${targetOverall} doesn't exist in Column C.`);
    }
  } catch (err: any) {
    console.error("❌ API ERROR:", err.message);
  }
  console.log("--- END Surgical Pick Update ---");
}

function capitalize(word: string) {
  if (!word) return '';
  return word.charAt(0).toUpperCase() + word.slice(1).toLowerCase();
}

function formatMessage(identity: string) {
  if (!identity || !identity.includes('|')) return identity;
  const [first, last, , off, def] = identity.split('|');
  const positionRaw = off || def || '';
  const position = positionRaw.toUpperCase();
  const firstName = capitalize(first);
  const lastName = capitalize(last);
  return `${position} - ${firstName} ${lastName}`;
}

function formatTimestamp(date: Date) {
  const mm = String(date.getMonth() + 1).padStart(2, '0');
  const dd = String(date.getDate()).padStart(2, '0');
  const yyyy = date.getFullYear();
  const hh = String(date.getHours()).padStart(2, '0');
  const min = String(date.getMinutes()).padStart(2, '0');
  const ss = String(date.getSeconds()).padStart(2, '0');
  return `${mm}/${dd}/${yyyy} ${hh}:${min}:${ss}`;
}

export async function logTransaction(tx: Transaction) {
  const timestamp = formatTimestamp(new Date());
  console.log(`[${timestamp}] Logging ${tx.type}: ${tx.identity}`);

  // --- 1. PREPARE MESSAGE ---
  let finalMessage = (tx.type === 'TRADE') ? tx.identity : (tx.type === 'INJURY PICKUP' && tx.details) ? tx.details : formatMessage(tx.identity);

  // --- 2. TEAM NAME LOOKUP ---
  let fullFrom = tx.fromTeam || 'FA';
  let fullTo = tx.toTeam || 'FA';
  
  try {
    const configRes = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'Config!A:B', 
    });
    const configRows = configRes.data.values || [];
    const teamMap = new Map(configRows.map(row => [row[1], row[0]]));
    
    if (tx.fromTeam && teamMap.has(tx.fromTeam)) fullFrom = teamMap.get(tx.fromTeam)!;
    if (tx.toTeam && teamMap.has(tx.toTeam)) fullTo = teamMap.get(tx.toTeam)!;
  } catch (err) {
    console.error("Team name lookup failed:", err);
  }

  // --- 3. EXECUTE PICK TRADE LOGIC ---
  if (tx.type === 'TRADE' && tx.identity.includes('#')) {
    await updatePickOwner(tx.identity, tx.toTeam || '', tx.fromTeam || '');
  }

  // --- 4. APPEND TO TRANSACTIONS TAB ---
  await sheets.spreadsheets.values.append({
    spreadsheetId: SHEET_ID,
    range: 'Transactions!A:J', 
    valueInputOption: 'RAW',
    requestBody: {
      values: [[
        timestamp,
        tx.type,
        finalMessage,             
        fullFrom,                 
        fullTo,                   
        tx.fromTeam || 'FA',      
        tx.toTeam || 'FA',        
        tx.coach,                 
        tx.status || 'INSTANT',   
        tx.weekBack || ''         
      ]],
    },
  });
}
</file>

<file path="app/cuts/page.tsx">
'use client';

import React, { useState, useEffect, useMemo } from 'react';
import PlayerCard from '@/components/PlayerCard'; 

interface GroupStats {
  avgAge: string | number;
  posMap: Record<string, number>;
  count: number;
}

interface TeamSummary {
  protected: number;
  pullback: number;
  lastUpdated: string;
}

interface Config {
  cuts_year: string;
  draft_year: string;
  protected: number;
  pullback: number;
  cuts_due_date: string; 
}

interface OrphanedPlayer {
  id: string;
  status: string;
  name: string;
}

function CountdownTimer({ dueDate }: { dueDate: string }) {
  const [timeLeft, setTimeLeft] = useState<string>('');
  const [isPast, setIsPast] = useState(false);

  useEffect(() => {
    if (!dueDate) return;
    const target = new Date(dueDate).getTime();
    const interval = setInterval(() => {
      const now = new Date().getTime();
      const distance = target - now;
      if (distance < 0) {
        setTimeLeft("SUBMISSIONS CLOSED");
        setIsPast(true);
        clearInterval(interval);
        return;
      }
      const days = Math.floor(distance / (1000 * 60 * 60 * 24));
      const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((distance % (1000 * 60)) / 1000);
      setTimeLeft(`${days}d ${hours}h ${minutes}m ${seconds}s`);
    }, 1000);
    return () => clearInterval(interval);
  }, [dueDate]);

  return (
    <div className="flex flex-col items-end">
      <span className="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">Deadline Countdown</span>
      <span className={`text-xl font-black tabular-nums tracking-tighter ${isPast ? 'text-red-500' : 'text-amber-500'}`}>
        {timeLeft || "INITIALIZING..."}
      </span>
    </div>
  );
}

export default function CutsPage() {
  const [teams, setTeams] = useState<any[]>([]);
  const [selectedTeam, setSelectedTeam] = useState('');
  const [roster, setRoster] = useState<any[]>([]);
  const [selections, setSelections] = useState<Record<string, string>>({});
  const [orphanedPlayers, setOrphanedPlayers] = useState<OrphanedPlayer[]>([]);
  const [summary, setSummary] = useState<Record<string, TeamSummary>>({});
  const [config, setConfig] = useState<Config>({ cuts_year: '', draft_year: '', protected: 30, pullback: 8, cuts_due_date: '' });
  
  const [searchTerm, setSearchTerm] = useState('');
  const [loading, setLoading] = useState(true);
  const [rosterLoading, setRosterLoading] = useState(false);
  const [saving, setSaving] = useState(false);
  const [viewingPlayer, setViewingPlayer] = useState<any>(null);

  const isExpired = useMemo(() => {
    if (!config.cuts_due_date) return false;
    return new Date().getTime() > new Date(config.cuts_due_date).getTime();
  }, [config.cuts_due_date]);

  const getTS = () => `ts=${new Date().getTime()}`;

  useEffect(() => {
    async function init() {
      try {
        setLoading(true);
        const configRes = await fetch(`/api/cuts/config?${getTS()}`, { cache: 'no-store' });
        const cfg = await configRes.json();
        setConfig(cfg);
        const [tRes, sRes] = await Promise.all([
          fetch(`/api/teams?${getTS()}`, { cache: 'no-store' }).then(r => r.json()),
          fetch(`/api/cuts?year=${cfg.cuts_year}&${getTS()}`, { cache: 'no-store' }).then(r => r.json())
        ]);
        setTeams(tRes);
        setSummary(sRes.summary || {});
      } catch (e) {
        console.error("Initialization error:", e);
      } finally {
        setLoading(false);
      }
    }
    init();
  }, []);

  useEffect(() => {
    if (!selectedTeam || !config.cuts_year) return;
    async function loadTeam() {
      setRosterLoading(true);
      try {
        const [pRes, cRes] = await Promise.all([
          fetch(`/api/players?team=${selectedTeam}&${getTS()}`, { cache: 'no-store' }).then(r => r.json()),
          fetch(`/api/cuts?team=${selectedTeam}&year=${config.cuts_year}&${getTS()}`, { cache: 'no-store' }).then(r => r.json())
        ]);
        const processedRoster = pRes.map((p: any) => ({
          ...p,
          identity: [p.first, p.last, p.age, p.offense, p.defense, p.special]
            .map(val => String(val || '').trim().toLowerCase())
            .join('|')
        }));
        const currentRosterIdentities = new Set(processedRoster.map((p: any) => p.identity));
        const orphaned = Object.entries(cRes.selections || {})
          .filter(([id, status]) => status !== 'cut' && !currentRosterIdentities.has(id))
          .map(([id, status]) => {
            const parts = id.split('|');
            return {
              id,
              status: String(status),
              name: `${parts[0]} ${parts[1]}`.toUpperCase()
            };
          });
        setOrphanedPlayers(orphaned);
        setRoster(processedRoster.sort((a: any, b: any) => (a.last || '').localeCompare(b.last || '')));
        setSelections(cRes.selections || {});
      } catch (e) {
        console.error("Load team error:", e);
      } finally {
        setRosterLoading(false);
      }
    }
    loadTeam();
  }, [selectedTeam, config.cuts_year]);

  const stats = useMemo(() => {
    const getStats = (type: string): GroupStats => {
      const rosterList = roster.filter(p => (selections[p.identity] || 'cut') === type);
      const orphanedList = orphanedPlayers.filter(p => p.status === type);
      const totalAge = rosterList.reduce((sum, p) => sum + (parseInt(p.age) || 0), 0);
      const posMap: Record<string, number> = {};
      rosterList.forEach(p => { 
        const pos = p.offense || p.defense || p.special || 'UNK';
        posMap[pos] = (posMap[pos] || 0) + 1; 
      });
      return {
        count: rosterList.length + orphanedList.length,
        avgAge: rosterList.length ? (totalAge / rosterList.length).toFixed(1) : 0,
        posMap
      };
    };
    return { protected: getStats('protected'), pullback: getStats('pullback'), cut: getStats('cut') };
  }, [roster, selections, orphanedPlayers]);

  const filteredRoster = roster.filter(p => 
    `${p.first} ${p.last}`.toLowerCase().includes(searchTerm.toLowerCase())
  );

  const handleToggle = (id: string, s: string) => {
    if (isExpired) return alert("Deadline passed. Submissions are locked.");
    const currentStatus = selections[id] || 'cut';
    if (s !== currentStatus) {
      if (s === 'protected' && stats.protected.count >= config.protected) return alert(`Limit ${config.protected} Protected.`);
      if (s === 'pullback' && stats.pullback.count >= config.pullback) return alert(`Limit ${config.pullback} Pullback.`);
    }
    setSelections(prev => {
      const newSelections = { ...prev, [id]: prev[id] === s ? 'cut' : s };
      if (newSelections[id] === 'cut' && orphanedPlayers.some(p => p.id === id)) {
        setOrphanedPlayers(prevO => prevO.filter(p => p.id !== id));
      }
      return newSelections;
    });
  };

  const fetchPlayerDetails = async (id: string) => {
    try {
      const r = await fetch(`/api/players/details/${id}`);
      if (r.ok) setViewingPlayer(await r.json());
      else alert("Could not load player stats.");
    } catch {
      alert("Error contacting scouting server.");
    }
  };

  const save = async () => {
    if (isExpired) return alert("Submissions are closed.");
    setSaving(true);
    try {
      const combined = [
        ...roster.map(p => ({ identity: p.identity, status: selections[p.identity] || 'cut' })),
        ...orphanedPlayers.map(p => ({ identity: p.id, status: p.status }))
      ];
      const res = await fetch('/api/cuts', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ team: selectedTeam, year: config.cuts_year, selections: combined })
      });
      if (res.ok) {
        const sRes = await fetch(`/api/cuts?year=${config.cuts_year}&${getTS()}`, { cache: 'no-store' }).then(r => r.json());
        setSummary(sRes.summary || {});
        alert("Roster saved.");
      }
    } catch { alert("Error connecting to server."); } finally { setSaving(false); }
  };

  if (loading) return (
    <div className="flex items-center justify-center min-h-screen bg-[#f8fafc] font-black text-slate-400 animate-pulse uppercase tracking-widest text-center px-4 text-sm">
      Establishing Secure Link...
    </div>
  );

  return (
    <div className="max-w-7xl mx-auto p-4 md:p-8 space-y-6 bg-[#f8fafc] min-h-screen font-sans">
      
      {/* 1. COMPLIANCE DASHBOARD */}
      <div className="bg-[#1e293b] rounded-[2rem] p-8 shadow-2xl border border-slate-700">
        <div className="flex justify-between items-start mb-8">
            <h2 className="text-blue-400 text-[10px] font-black uppercase tracking-[0.4em] flex items-center gap-2">
                <span className="w-2 h-2 bg-blue-500 rounded-full animate-ping"></span>
                League Compliance Monitor — {config.cuts_year} Season
            </h2>
            {config.cuts_due_date && <CountdownTimer dueDate={config.cuts_due_date} />}
        </div>
        <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-5">
          {teams.map(t => {
            const s = summary[t.short] || { protected: 0, pullback: 0 };
            const isComplete = s.protected === config.protected && s.pullback === config.pullback;
            return (
              <div key={t.short} className={`p-5 rounded-2xl border-2 transition-all duration-300 ${isComplete ? 'bg-emerald-500/10 border-emerald-500/40' : 'bg-slate-800/40 border-slate-700'}`}>
                <p className={`text-[12px] font-black uppercase mb-3 truncate ${isComplete ? 'text-emerald-400' : 'text-slate-200'}`}>{t.name}</p>
                <div className="space-y-2">
                  <div className="flex justify-between items-center text-[10px] font-bold">
                    <span className="text-slate-500 uppercase tracking-wider">Protected</span>
                    <span className={`px-2 py-0.5 rounded ${isComplete ? 'text-emerald-300 bg-emerald-500/20' : 'text-white'}`}>{s.protected} / {config.protected}</span>
                  </div>
                  <div className="flex justify-between items-center text-[10px] font-bold">
                    <span className="text-slate-500 uppercase tracking-wider">Pullback</span>
                    <span className={`px-2 py-0.5 rounded ${isComplete ? 'text-emerald-300 bg-emerald-500/20' : 'text-white'}`}>{s.pullback} / {config.pullback}</span>
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </div>

      <div className="flex flex-col md:flex-row justify-between items-end gap-6 pt-4">
        <div className="space-y-1">
          <h1 className="text-5xl font-black italic uppercase tracking-tighter text-slate-900 leading-none">Cuts Portal</h1>
          <p className="text-[11px] font-black text-slate-400 uppercase tracking-[0.2em] italic">Authorized Access: {config.draft_year} Season</p>
        </div>
        <div className="w-full md:w-96 group">
          <label className="text-[10px] font-black text-slate-400 uppercase mb-2 block ml-1 tracking-widest">Select Franchise</label>
          <select 
            value={selectedTeam} 
            onChange={(e) => setSelectedTeam(e.target.value)} 
            className="p-5 border-2 border-slate-200 rounded-3xl font-black bg-white w-full shadow-xl outline-none focus:border-blue-500 transition-all appearance-none cursor-pointer text-slate-800"
          >
            <option value="">Choose a team...</option>
            {teams.map(t => <option key={t.short} value={t.short}>{t.name}</option>)}
          </select>
        </div>
      </div>

      {selectedTeam && (
        <>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8 pt-4">
            <StatCard title="Protected" stats={stats.protected} color="text-emerald-500" border="border-emerald-500" />
            <StatCard title="Pullback" stats={stats.pullback} color="text-blue-500" border="border-blue-500" />
            <StatCard title="Released" stats={stats.cut} color="text-red-500" border="border-red-500" />
          </div>

          <div className="sticky top-6 z-50 bg-[#0f172a] shadow-[0_20px_50px_rgba(0,0,0,0.3)] p-5 rounded-[2.5rem] flex flex-col md:flex-row justify-between items-center px-10 border border-slate-700 gap-6">
             <div className="flex gap-12 text-white font-black">
                <div className="flex flex-col">
                  <span className="text-[10px] text-slate-500 uppercase tracking-[0.2em] mb-1">Protected</span>
                  <span className={`text-3xl tracking-tighter ${stats.protected.count === config.protected ? 'text-emerald-400' : 'text-white'}`}>
                    {stats.protected.count}<span className="text-slate-600 text-lg ml-1">/ {config.protected}</span>
                  </span>
                </div>
                <div className="flex flex-col">
                  <span className="text-[10px] text-slate-500 uppercase tracking-[0.2em] mb-1">Pullback</span>
                  <span className={`text-3xl tracking-tighter ${stats.pullback.count === config.pullback ? 'text-blue-400' : 'text-white'}`}>
                    {stats.pullback.count}<span className="text-slate-600 text-lg ml-1">/ {config.pullback}</span>
                  </span>
                </div>
             </div>

             <div className="flex-1 max-w-xl w-full">
                <div className="relative">
                  <input 
                    type="text" 
                    placeholder="Search roster..." 
                    value={searchTerm}
                    onChange={(e) => setSearchTerm(e.target.value)}
                    className="w-full bg-slate-800/50 border border-slate-600 rounded-2xl px-6 py-4 text-white font-bold text-sm outline-none focus:border-blue-400 focus:bg-slate-800 transition-all"
                  />
                </div>
             </div>

             <button 
               onClick={save} 
               disabled={saving || rosterLoading || isExpired} 
               className={`px-12 py-5 rounded-2xl font-black uppercase text-xs transition-all shadow-xl active:scale-95 flex items-center gap-3 min-w-[200px] justify-center ${isExpired ? 'bg-red-900 text-red-400 cursor-not-allowed' : 'bg-blue-600 hover:bg-blue-500 text-white'}`}
             >
               {isExpired ? 'CLOSED' : saving ? 'Syncing...' : 'Submit Final Cuts'}
             </button>
          </div>

          {orphanedPlayers.length > 0 && (
            <div className="bg-amber-50 border-2 border-amber-200 rounded-[2rem] p-6 mb-6">
              <p className="text-amber-700 text-xs font-bold mb-4 italic uppercase tracking-wider">Traded players detected. Release these slots to regain compliance.</p>
              <div className="grid gap-3">
                {orphanedPlayers.map((p) => (
                  <div key={p.id} className="bg-white/50 p-4 rounded-xl flex justify-between items-center border border-amber-100">
                    <span className="text-slate-900 font-black text-sm uppercase">{p.name} ({p.status})</span>
                    <button onClick={() => handleToggle(p.id, p.status)} className="text-[10px] font-black uppercase bg-white border border-amber-300 text-amber-600 px-4 py-2 rounded-lg hover:bg-amber-600 hover:text-white transition-all">Release Slot</button>
                  </div>
                ))}
              </div>
            </div>
          )}

          <div className="grid grid-cols-1 gap-4 pb-20">
            {filteredRoster.map((p) => {
              const s = selections[p.identity] || 'cut';
              return (
                <div key={p.identity} className="bg-white p-6 rounded-[2rem] shadow-sm border border-slate-100 flex flex-col sm:flex-row justify-between items-center group hover:shadow-xl transition-all gap-4">
                  <div className="text-center sm:text-left flex-1">
                    <div className="flex items-center gap-4">
                        {/* --- ADDED GOOGLE SEARCH ON CLICK --- */}
                        <h3 
                          onClick={() => {
                            const query = encodeURIComponent(`${p.first} ${p.last} `);
                            window.open(`https://www.google.com/search?q=${query}`, '_blank');
                          }}
                          className="font-black text-2xl text-slate-800 uppercase leading-none tracking-tight cursor-pointer hover:text-blue-600 hover:underline transition-all"
                          title="Search "
                        >
                          {p.first} {p.last}
                        </h3>
                        <button 
                         onClick={() => fetchPlayerDetails(p.identity)}
                         className="bg-slate-100 hover:bg-blue-500 hover:text-white text-slate-400 text-[9px] font-black px-3 py-1 rounded-full uppercase transition-all"
                        >
                          View Stats
                        </button>
                    </div>
                    <div className="flex items-center gap-3 mt-2 justify-center sm:justify-start">
                      <span className="text-[11px] font-black text-blue-600 bg-blue-50 px-2 py-0.5 rounded-md uppercase">{p.offense || p.defense || p.special || 'UNK'}</span>
                      <span className="text-[11px] font-bold text-slate-400 uppercase tracking-widest">Age {p.age}</span>
                    </div>
                  </div>
                  <div className="flex gap-3 w-full sm:w-auto">
                    <StatusBtn label="Protect" active={s === 'protected'} color="bg-emerald-500" onClick={() => handleToggle(p.identity, 'protected')} />
                    <StatusBtn label="Pullback" active={s === 'pullback'} color="bg-blue-500" onClick={() => handleToggle(p.identity, 'pullback')} />
                    <StatusBtn label="Cut" active={s === 'cut'} color="bg-red-500" onClick={() => handleToggle(p.identity, 'cut')} isCutType />
                  </div>
                </div>
              );
            })}
          </div>
        </>
      )}

      {viewingPlayer && (
        <PlayerCard 
          data={viewingPlayer} 
          onClose={() => setViewingPlayer(null)} 
        />
      )}
    </div>
  );
}

function StatCard({ title, stats, color, border }: any) {
  return (
    <div className={`bg-white p-8 rounded-[2.5rem] border-t-[14px] ${border} shadow-lg space-y-6`}>
      <div className="flex justify-between items-start">
        <p className="text-[12px] font-black text-slate-400 uppercase tracking-[0.2em]">{title}</p>
        <div className="text-right">
          <p className={`text-5xl font-black italic tracking-tighter leading-none ${color}`}>{stats.count}</p>
          <p className="text-[10px] font-black text-slate-400 uppercase mt-2 tracking-widest">Avg Age {stats.avgAge}</p>
        </div>
      </div>
      <div className="pt-4 border-t border-slate-50 flex flex-wrap gap-2">
        {Object.entries(stats.posMap).map(([pos, count]: any) => (
          <span key={pos} className="bg-slate-50 text-slate-600 px-3 py-1.5 rounded-xl text-[10px] font-black uppercase border border-slate-100">{pos} {count}</span>
        ))}
      </div>
    </div>
  );
}

function StatusBtn({ label, active, color, onClick, isCutType }: any) {
  const base = "flex-1 sm:flex-none px-8 py-4 rounded-2xl text-[11px] font-black uppercase border-2 transition-all min-w-[110px]";
  if (active) return <button onClick={onClick} className={`${base} ${color} text-white border-transparent shadow-xl scale-105 z-10`}>{label}</button>;
  return <button onClick={onClick} className={`${base} bg-white ${isCutType ? 'text-red-500 border-red-50' : 'text-slate-300 border-slate-100'}`}>{label}</button>;
}
</file>

<file path="app/api/draft-picks/route.ts">
import { NextResponse } from 'next/server';
import { sheets, SHEET_ID } from '@/lib/googleSheets'; // Import direct sheets access
import { transferDraftPick } from '@/lib/draftPicks';

export const dynamic = 'force-dynamic';
export const revalidate = 0;

export async function GET() {
  try {
    // We bypass the lib function here to ensure we get EXACTLY columns A through H
    const response = await sheets.spreadsheets.values.get({
      spreadsheetId: SHEET_ID,
      range: 'DraftPicks!A:H', 
    });

    const rows = response.data.values;
    if (!rows || rows.length === 0) return NextResponse.json([]);

    // Map the rows to objects including the new G and H columns
    const formattedPicks = rows.slice(1).map((p: any) => ({
      year: p[0] || '',
      round: p[1] || '',
      overall: p[2] || '',
      originalTeam: p[3] || '',
      currentOwner: p[4] || '',
      status: p[5] || '',
      draftedPlayer: p[6] || '', // Column G
      timestamp: p[7] || ''      // Column H
    }));

    return NextResponse.json(formattedPicks);
  } catch (error) {
    console.error('API /draft-picks GET failed:', error);
    return NextResponse.json(
      { error: 'Failed to load draft picks' },
      { status: 500 }
    );
  }
}

// KEEPING YOUR TRADE LOGIC BELOW
export async function POST(req: Request) {
  try {
    const body = await req.json();
    const { fromTeam, toTeam, year, round, overall } = body;

    if (!fromTeam || !toTeam || !year || !round) {
      return NextResponse.json(
        { error: 'Missing required fields' },
        { status: 400 }
      );
    }

    //@ts-ignore
    await transferDraftPick(
      fromTeam,
      toTeam,
      Number(year),
      Number(round),
      overall ? Number(overall) : undefined
  );

    return NextResponse.json({ success: true });
  } catch (error) {
    console.error('API /draft-picks POST failed:', error);
    return NextResponse.json(
      { error: 'Draft pick transfer failed' },
      { status: 500 }
    );
  }
}
</file>

</files>
